# 09 — IMPLEMENTATION SEQUENCE DETERMINATION

**Mission:** IAC-001 | **Date:** 2026-07-23
**Mode:** Read-only. No implementation, no commits, no tags, no push.
**Sources:** `40-EXECUTION-WAVE-REGISTER.md`, `38-DEPENDENCY-REGISTER.md` (engine-generated by `phase3_engine.py`); reference chain in `03-CATALOGS/`

---

## Determination: **COMPLETE**

A deterministic implementation sequence **is fully derivable from repository evidence**. It is engine-generated from the acyclic dependency graph — not hand-authored — so it is reproducible and order-stable.

---

## Basis of Determinability

- Dependency graph is a **strict partial order**, **acyclic by construction** (artifact 05).
- The Execution Wave Register is a **topological partition** of that graph.
- The reference-universe chain **Data → Event → API → Workflow → Service → Application** provides a second, corroborating acyclic ordering for the un-realized layers.

Because both orderings are acyclic and engine-derived, the implementation sequence is **deterministic**: any conformant scheduler reproduces the same wave assignment.

---

## Engine-Derived Wave Schedule

| Wave | Units | Notes |
|------|-------|-------|
| W1 | 2 | foundational layer classes |
| W2 | 2 | |
| W3 | 8 | |
| W4 | 0 | |
| W5 | 2 | |
| W6 | 0 | |
| W7 | 0 | |
| WF | 96 | **deferred, authorization-gated** (DEFERRED disposition) |

---

## Derived Realization Order for the Un-Realized Span

From the reference chain (**Data → Event → API → Workflow → Service → Application**), with Data/Service/Application/Infrastructure already REALIZED+CERTIFIED, the deterministic remaining order is:

1. **Event layer** — build `event.py` factory + Event EC-3 realization band (~612 assets). *(Event precedes API in the chain.)*
2. **API layer** — realize 765 APIs + 765 contracts against the existing `api` artifact factory + a new API realization band. *(API depends on Event.)*
3. **Workflow layer** — build `workflow.py` factory + Workflow EC-3 realization band (~612 assets). *(Workflow depends on API.)*
4. **Re-run `verify.sh` + closure** — regenerate validation/certification/traceability evidence for the newly realized span; close the Band-13 U12 freeze residual.
5. **Route DR-RAT-11** — submit the constitutional elevation act to the out-of-corpus ratifying authority (see artifact 07).

This order is a **direct consequence** of the acyclic reference chain; steps 1–3 cannot be reordered without violating the dependency partial order.

---

## Conclusion

Implementation Sequence Determination is **COMPLETE**. The sequence is deterministic, engine-derived, and reproducible. This domain imposes **no blocker** on certification; it supplies the remediation order used in artifact 10.
