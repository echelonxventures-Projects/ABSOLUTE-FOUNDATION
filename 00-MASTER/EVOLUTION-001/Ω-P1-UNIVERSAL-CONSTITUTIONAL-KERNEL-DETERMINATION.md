# Ω-P1 — UNIVERSAL CONSTITUTIONAL KERNEL DETERMINATION

> **STANDING: ADMITTED TO REPOSITORY TRUTH.**
> Admitted under Ω-E06-B through the located registration transaction, in the canonical home
> proven at §0 — `00-MASTER/EVOLUTION-001/`, **REUSE**, no new home, no new namespace, no new
> identifier. `00-MASTER/` is excluded from corpus registration by
> `00-MASTER/UCOS-RECON-C1-OPERATIONAL-MEMORY-EXCLUSION.md` and `00-BOOK/tools/config.py`
> `EXCLUDE_DIR_PREFIXES`, so admission consumes no corpus identity and introduces no
> registration drift. The disposition is recorded in the located decision-assimilation register
> `00-MASTER/UCDA-000001/ucda-decisions.json` as `DEC-OMEGA-P-01` and indexed at
> `00-MASTER/MCP-004-MASTER-DECISIONS.md` §05.
>
> **AUTHORITY IS UNCHANGED — NONE (DERIVED TRUTH).** Admission changes standing, never
> authority: where this determination and a located instrument differ, **the located instrument
> governs**. Every measurement below is an epoch measurement taken at the program state named in
> the header; admission neither restates nor amends any of them.
>
> The discharged candidate banner read: *"STANDING: PROVISIONAL — NOT YET REPOSITORY TRUTH.
> This artifact is deliberately untracked by version control … Until then it is a measurement
> offered for adjudication, not a record."* Its stated condition — acceptance of canonical
> ownership plus the located registration transaction — is now satisfied.

| Field | Value |
|---|---|
| Mission | Ω-P1 — Universal Constitutional Admission & Completeness |
| Class | Constitutional determination (measurement only) |
| AUTHORITY | **NONE — DERIVED TRUTH.** Legislates nothing; opens no registry; mints no identifier; declares no namespace; admits no capability; certifies nothing; creates no lifecycle, ownership topology or authority. Where this determination and a located instrument differ, **the located instrument governs**. |
| LAW OWNER | `00-CMG/CMG-000001-CONSTITUTIONAL-META-GOVERNANCE-CONSTITUTION.md` |
| ADMISSION OWNER | `00-CEP/CEP-009` ADDENDUM B (`CEP-009-AMD-001`) |
| OWNERSHIP-PATTERN OWNER | `CMG-000001` **Article LXXXII** (Delegation Register) — REUSED, not restated |
| KERNEL OWNER | `engine/kernel/` v1.0.0, bound by `00-MASTER/UMK-000001/umk-kernel.json` |
| LIFECYCLE OWNER | `00-MASTER/UCL-000001/ucl-stage-manifest.json` |
| MEASURED AT | HEAD `43dec4a`, branch `integration/recovery-001`, working tree clean |
| FIXED POINT | **CERTIFIED** — `UCOS-RFP-001: REPOSITORY IS A FIXED POINT \| passes=3 \| stages=35 \| criteria=13/13 \| cycles=0 \| gate=OPEN` |
| DETERMINATION | **KERNEL LOCATED AND SUBSTANTIALLY COMPLETE — 7 EXTEND · 1 RECORD AS GAP · 0 CREATE · 1 FINDING WITHDRAWN · FIXED POINT CERTIFIED** |

---

## 0. Canonical ownership of this artifact — proven, then held provisional

This determination is itself constitutional knowledge and is bound by the same
REUSE→EXTEND→CREATE rules it applies. Ownership was therefore determined **before**
persistence, and the disposition of the artifact is **HOLD** pending acceptance.

| Step | Finding |
|---|---|
| Constitutional authority | `CMG-DLG-17` (Art LXXXII.2) — *"Repository Truth, knowledge content, canonical knowledge objects, canonical homes, knowledge graph, decision store"* → owner `00-BOOK/` (UKB), *"UKB as sole owner of Repository Truth"*. `CMG-DLG-13` → `REG-AUTO-001` owns registration, identity allocation and classification. |
| Constitutional intent | Repository Truth is owned in one place; a determination that *records a measurement* is derived truth, not law. |
| Located exclusion | `UCOS-RECON-C1`, implemented at `00-BOOK/tools/config.py` `EXCLUDE_DIR_PREFIXES` → `"00-MASTER/"`: operational memory *"is execution state, not corpus: it must never consume permanent corpus identities."* Verified: `00-MASTER/` is excluded. |
| Located precedent 1 | `ADR-0003` is homed inside `00-MASTER/UMK-000001/` rather than the corpus `adr/` family, for the stated reason that *"the programme is AUTHORITY = NONE (derived truth); homing the record here … introduces no corpus-registration drift."* |
| Located precedent 2 | `Ω-E03`, `Ω-E04`, `Ω-E05` `CONSTITUTIONAL-COMPLETION-CERTIFICATE.md` are all homed in `00-MASTER/EVOLUTION-001/` and are all tracked (verified via `git ls-files`), each `AUTHORITY = NONE`, each recording a measurement, each carrying *"where this certificate and a located instrument differ, the located instrument governs."* |
| Canonical owner | `00-MASTER/EVOLUTION-001/` — the located home of evolution-round constitutional determinations. **REUSE.** No new programme home, no new namespace, no new identifier. |
| Disposition of this artifact | **ADMITTED** (Ω-E06-B). Recorded as `DEC-OMEGA-P-01` in `00-MASTER/UCDA-000001/ucda-decisions.json`, disposition `IMPLEMENTED` at stage `REPOSITORY-TRUTH-UPDATE`. The prior disposition was **HOLD** (`CMG-000001` LXXVII.5) — left untracked so that it could not become Repository Truth by default routing (LXXVII.4) — and it is discharged by acceptance of the canonical ownership proven above together with the located registration transaction. |

**No CREATE was required to home this determination.**

---

## 1. Determination discipline actually applied

Constitutional conclusions are drawn from Repository Truth. Implementations supply evidence
of conformance and never supply law. Each determination below followed:

1. Locate constitutional authority → 2. Locate canonical owner → 3. Locate Repository Truth
representation → 4. Determine constitutional intent → 5. Locate implementation(s) →
6. Measure implementation conformance → 7. Produce evidence.

Two of this determination's own earlier findings were produced by inverting that order and
are corrected here: one is **withdrawn** (§4, Ω-P1-D-08) and one is **restated as a
conformance measurement** rather than a structural observation (Ω-P1-D-07). Both corrections
are recorded rather than silently applied.

### 1.1 The derivation order of the kernel

| # | Plane | Located owner | Resolves |
|---|---|---|---|
| 1 | Principles | `00-CEP/CEP-000`, `CEP-001`; `CMG-000001` Art IX (`CMG-P-*`) | YES |
| 2 | Laws | `CMG-000001` Art X — `CMG-L-01…14` | YES |
| 3 | Invariants | `CMG-000001` Art XI — `CMG-INV-01…12` (constitutionally **closed**, XI.13) | YES |
| 4 | Kernel | `engine/kernel/` (10 substrate modules), bound by `UMK-000001` | YES |
| 5 | Mechanisms | Delegated under Art LXXXII; crosswalked to meta-types by `umk-kernel.json` | YES |
| 6 | Applicability | `ucl-declaration.json:applicability_classes` + `obligation_rules` | YES |
| 7 | Lifecycle | `ucl-stage-manifest.json` (45 nodes) | YES |
| 8 | Canonical owner discovery | `CMG-000001` LXXVI.2(a),(d) + Art XVIII + Art LXXXII | YES |
| 9 | Disposition | `CMG-000001` LXXVII.2 (five outcomes) + LXXVII.5 (HOLD) | YES |

---

## 2. Ownership topology — REUSED from Article LXXXII, not derived

**Repository Truth already provides the universal delegation pattern. It is `CMG-000001`
Article LXXXII, and it is REUSED here in full.** No ownership topology is derived, named or
proposed by this determination. Any taxonomy of "centralized / distributed / federated /
synthesized / derived" ownership would be a second ownership model over a concern Art LXXXII
already owns, and is therefore refused under `CMG-L-14` and `LXXXII.1`.

Art LXXXII is the canonical pattern by its own normative text, which covers every ownership
mode in question:

| Ownership mode | Located clause |
|---|---|
| **Reference ownership** | `CMG-P-04` (IX.4) — *"Delegation before legislation. An existing owner is bound by reference, never re-legislated."* |
| **Delegation record contract** | XVIII.2 — a record SHALL carry *exactly*: identifier, concern, owner, basis, disposition (EXTEND or REUSE), revocation condition. *"A record missing any element IS void (CMG-L-07)."* |
| **Delegation fidelity** | `CMG-L-07` (X.7) — a delegation *"SHALL create no authority"*; delegation to an unlocated owner is void → vacancy. |
| **Distributed ownership with injectivity** | Cardinality table: `Delegation → Owner  1 : 1`, basis `CMG-L-07`; plus `CMG-INV-02` (XI.2) concern→owner injectivity. |
| **Crosswalk / federated, never merged** | LXXXII.7 — *"Each entry IS a recognition… Deletion of an entry would not diminish the owner's authority."* No entry *"confers, alters, limits, or conditions"* the owner's authority. |
| **Zero Parallel Authority** | LXXXII.1 — *"it IS the mechanism by which this instrument achieves Zero Parallel Authority"*; `CMG-L-14` (X.14) No Parallel Machinery. |
| **Located-owner verification** | LXXXII.4 — every delegation SHALL be verified to resolve to a located owner (validator `L.3(f)`, `LXXX.2` precondition 14); unlocated → void → vacancy (XVIII.6). |
| **Residue computation** | LXXXII.3 — 11 retained concerns `CMG-RET-01…11`; `CMG-RET-05` retains *"Allocation of concerns to owners; the Delegation Register; Concern-to-Owner injectivity."* |
| **Maintenance obligation** | LXXXII.5 — admission, retirement or transfer SHALL update the Article by amendment in the same change. |
| **Gap routing** | LXXXII.6 — a concern absent from LXXXII.2 and LXXXII.3 **and** unowned IS a gap under Art LXXVIII. |

**Consequence for the mechanism plane.** The mission's question *"is there a Universal
Constitutional Mechanism plane?"* is answered by Art LXXXII: mechanisms are **delegated to
located owners and bound by reference**. A single central mechanism manifest is not missing —
it is **constitutionally prohibited**, because creating one would re-legislate delegated
concerns (LXXXII.1) and constitute parallel machinery (`CMG-L-14`).

**Measured conformance of the pattern.** The delegation register is implemented in
machine-readable form as `00-CMG/CMG-REGISTRY.json:concerns` — **60 records** with IDs
`CMG-DLG-*` (49, disposition `REUSE`) and `CMG-RET-*` (11, disposition `RETAIN`), each
carrying `{id, concern, owner, disposition, basis}` (+ `owner_paths` where the owner is a
path). `00-CMG/tools/cmg_validate.py:check_concerns` enforces, per record: `CMG-INV-02`
injectivity; `CMG-L-07` basis presence; `CMG-INV-03` exactly one **located** owner (`XVIII.6`
"delegates to unlocated owner", plus `owner_paths` existence checks — i.e. **LXXXII.4 is
implemented**); `XX.8` refusal of a `DECLARATIVE` owner; `LXXXII.3`/`CMG-INV-12` the
RETAIN/REUSE partition; and orphan governance (an artifact of binding standing owning no
concern). The pattern is therefore **substantially conformant**, not merely declared. Two
precise non-conformances remain, at Ω-P1-D-02 and Ω-P1-F-01.

---

## 3. The Universal Constitutional Kernel exists

**Constitutional authority.** `CMG-000001` XIII.2: *"The Kind set IS OPEN (`CMG-P-09`). A new
Kind SHALL be admitted under Article LXXVI and SHALL NOT require amendment of any existing
Kind. Admission of a Kind IS an append operation."* LXXVI.1 lists the admissible kinds;
LXXVI.3 makes admission append-only; LXXVI.5 makes expansion unbounded in count with *"any
apparent limit … read as a defect."* `CEP-009` B.3.3 and B.5.2 restate it for construct
classes and architectural axes.

**Intrinsic finiteness is separately and exhaustively declared.**
`CMG-REGISTRY.json:closed_enumerations` names exactly **four** constitutionally closed
domains — `identifier_families` (V.3), `invariants` (XI.13), `lifecycle_phases` (XXIV.3),
`unknown_concept_dispositions` (LXXVII.3) — each with its article, its closing invariant
`CMG-INV-09`, and a stated reason. Verified: `kinds` (24), `namespaces` (17),
`relationship_types` (16) and `states` (14) are **not** closed. This is precisely the
*Intrinsic Finite Domains vs Constitutionally Open Domains* distinction the mission requires,
and it already has a canonical owner.

**Located mechanism.** `engine/kernel/` — 10 required substrate modules, all resolving. One
universal thing (`MetaObject`), one reflective root (`MetaType`, classified by itself), open
registration, universal governance (`Policy`/`Constraint`, extensible), deterministic
identity, append-only versioning, Knowledge-Once, tamper-evident hash-chained journal. The 22
founding abstractions are a DATA seed table treated as any unseen category. Six blocking
quality gates, including `no-closed-registries` and `unknown-future-compatibility`.

**Measured conformance to XIII.2.** `umk_engine.py --gate` → `CONSTITUTIONALLY-COMPLIANT`.
`umk_engine.py:check_declaration` measures every declared abstraction against
`MetaKernel().registry.metatype_keys()` — the kernel's **registered, data-derived** category
set — so the 22 abstractions are a *crosswalk to registered meta-types* (an Art LXXXII
recognition), not an ontology. `compliance.py:_kernel_has_no_closed_enum()` AST-parses every
`engine/kernel/*.py` and fails on any `Enum` subclass; independently verified zero hits.
Evidence of conformance, obtained by probe: a category named in no seed table, no enum and no
declaration was admitted by registration alone; the registered set grew 23 → 25; no kernel
source was edited. The probe was in-memory and persisted nothing.

**Verdict: the kernel CONFORMS to XIII.2.** Zero Hard Coding and Zero Artificial Boundaries
hold at the substrate, by executed proof.

### 3.1 The kernel's constitutional population

Measured at HEAD: the kernel registry holds **23 registered heads, of which 0 are
non-metatype objects**. Repository Truth simultaneously holds 447 constitutional concepts,
1,194 registered artifacts, 12,821 relationships, 45 lifecycle stages, 26 gates, 47 checks
and 35 constitutional engines. Only 3 of 35 engines reference the kernel at all.

**This is reported, not condemned.** Under Art LXXXII the kernel is *not* required to be a
central store of every constitutional object — each concern is held by its delegated owner,
and a kernel that absorbed them would be the parallel machinery `CMG-L-14` forbids. The
correct constitutional question is therefore not *"why is the kernel empty?"* but *"does
every constitutional object participate in every applicable mechanism through its delegated
owner?"* That question is answered at Ω-P1-D-04, which is consequently the **primary**
finding of this determination.

---

## 4. Mechanism census — every Universal Constitutional Mechanism dispositioned

Mechanism set derived from Repository Truth's own declarations: Art LXXXII.2 delegations
(49) + LXXXII.3 retained concerns (11) + `umk-kernel.json:universal_abstractions` (22) +
`CEP-009` B.6 participation properties (16) + `ucl-declaration.json:obligation_rules` (11).

**PASS** owner located, mechanism present, conformant · **REUSE** consume unchanged ·
**EXTEND** owner located, conformance or coverage incomplete · **GAP** substantive and
unowned (LXXVII.2(d)) · **HOLD** undetermined (LXXVII.5) · **REJECT** not constitutional.

### 4.A Constitutional ground

| # | Mechanism | Canonical owner | Disp. |
|---|---|---|---|
| A-01 | Principles (`CMG-P-*`) | `CMG-000001` Art IX; `CEP-000`, `CEP-001` | PASS |
| A-02 | Laws (`CMG-L-01…14`) | `CMG-000001` Art X | PASS |
| A-03 | Invariants (`CMG-INV-01…12`, closed by XI.13) | `CMG-000001` Art XI + validator Art L | PASS |
| A-04 | Intrinsic-finiteness proof (exactly 4 closed domains) | `CMG-REGISTRY.json:closed_enumerations` + `CMG-INV-09` | PASS |
| A-05 | Total disposition rule (5 outcomes, exhaustive by construction) | `CMG-000001` LXXVII.2/.3 + LXXVII.5 HOLD | PASS |
| A-06 | Admission procedure (8 steps a–h) | `CMG-000001` LXXVI.2 | PASS |
| A-07 | Append-only admission | `CMG-000001` LXXVI.3 | PASS |
| A-08 | Unbounded expansion in count | `CMG-000001` LXXVI.5 | PASS |
| A-09 | No admission by reinterpretation | `CMG-000001` LXXVI.6; `CEP-009` B.3.4 | PASS |
| A-10 | No default routing of unknown concepts | `CMG-000001` LXXVII.4 | PASS |
| A-11 | Substrate independence | `CMG-000001` LXXVII.7; `CEP-009` B.5.4 | PASS |
| A-12 | Open Kind set | `CMG-000001` XIII.2 (`CMG-P-09`) | PASS |

### 4.B Ownership and Zero Parallel Authority — Art LXXXII, REUSED

| # | Mechanism | Canonical owner | Disp. |
|---|---|---|---|
| B-01 | Universal delegation pattern | `CMG-000001` **Art LXXXII** | **REUSE** |
| B-02 | Delegation before legislation | `CMG-P-04` (IX.4) | PASS |
| B-03 | Delegation record contract | `CMG-000001` XVIII.2 | **EXTEND** (Ω-P1-D-02) |
| B-04 | Delegation fidelity, no authority conferred | `CMG-L-07` (X.7); LXXXII.7 | PASS |
| B-05 | No parallel machinery | `CMG-L-14` (X.14) | PASS |
| B-06 | Concern→owner injectivity | `CMG-INV-02` (XI.2) + `cmg_validate.py:check_concerns` | PASS |
| B-07 | Located-owner verification | LXXXII.4 + XVIII.6, implemented in `check_concerns` | PASS |
| B-08 | Orphan-governance detection | `CMG-INV-03` + `check_concerns` (binding standing owning no concern) | PASS |
| B-09 | Retained-residue partition | LXXXII.3 + `CMG-INV-12` | PASS |
| B-10 | Register maintenance obligation | LXXXII.5 | PASS |
| B-11 | Gap routing for unowned concerns | LXXXII.6 → Art LXXVIII | PASS |
| B-12 | Concept canonical home | `UAKOS-CLOSURE-002` — 447 concepts, `duplicate_canonical_homes = 0`, `orphan_concepts = 0`, `not_homed_concepts = 0`, CLOSED | PASS |
| B-13 | Machine-generated ownership register | `UAKOS-CLOSURE-002/31-CONCEPT-OWNERSHIP-REGISTER.md` (447 / 11 zones) | PASS |
| B-14 | Semantic ownership overlap | `engine/knowledge/integration/ownership.py:find_overlaps` (`UKI-LAW-005`) | **EXTEND** |
| B-15 | Narrative ownership matrix | `02-CANONICAL-OWNERSHIP-MATRIX.md` | **EXTEND** (Ω-P1-D-06) |
| B-16 | Constitutional authority framework | `UCOS-UCAF-001`, gate G-18 | PASS |

### 4.C Kernel substrate — 22 abstractions

All 22 declared `mechanism` paths resolve; all 22 `metatype` values are registered in the
kernel. Mechanism disposition **PASS** for all 22 (`UMK-ABS-01…22`); `UMK-ABS-07` (Universal
Contract) is **REUSE** of `engine/foundation/contracts/contract.py`. Their *application* to
the constitutional population is delegated, and is measured at Ω-P1-D-04, not here.

### 4.D Admission and evolution

| # | Mechanism | Canonical owner | Disp. |
|---|---|---|---|
| D-01 | **Universal Constitutional Admission Contract** | `CEP-009` **ADDENDUM B** | **REUSE** |
| D-02 | Primary Law of Evolution (no redesign) | `CEP-009` B.2.1–B.2.4 | PASS |
| D-03 | Open register of construct classes | `CEP-009` B.3.2 + B.3.3 | PASS |
| D-04 | 15-stage evolution lifecycle, each stage owned | `CEP-009` B.4.1–B.4.5 | PASS |
| D-05 | Lawful termination by explicit disposition | `CEP-009` B.4.6 | PASS |
| D-06 | Executable 45-stage lifecycle graph | `UCL-000001`, G-25 OPEN, 45/45 ordered, 0 cycles, all owners resolve | PASS |
| D-07 | Applicability determination | `ucl-declaration.json:applicability_classes` — REQUIRED / CONDITIONAL / NOT_APPLICABLE | PASS |
| D-08 | Evidence of non-applicability | `UCL-000001/02-STAGE-OBLIGATION-BINDING-MATRIX.md` | PASS |
| D-09 | Obligation binding over stages | `ucl-declaration.json:obligation_rules` (11) | **EXTEND** (Ω-P1-D-05) |
| D-10 | 16 participation properties | `CEP-009` B.6 — all 16 discharging owners resolve; B.6.2/B.6.3 fail-closed | **EXTEND** |
| D-11 | Redesign prohibition | `CEP-009` B.7.1–B.7.4 | PASS |
| D-12 | Reuse before create | `CMG-000001` Art XL + XLI.7; executable in `ucef`, `mcos`, `urrc` engines | PASS |
| D-13 | Disposition record | `UCDA-000001/ucda-decisions.json` — 108 decisions (42 / 33 / 18 / 11 / 4 `SUPERSEDED`) | PASS |
| D-14 | Universal artifact admission | `00-BOOK/tools/ukb.py` — universe is `git ls-files --cached`; any new file discovered with zero configuration; fail-closed | PASS |
| D-15 | Registration transaction + drift gate | `register.sh --guard` (`CK-REG-DRIFT`, exit 3) | PASS |

**D-01 is the mission's headline result.** The Universal Constitutional Admission Contract
the mission asks to be *produced* already exists normatively — B.2 (law), B.3 (open construct
register), B.4 (15 owned stages), B.5 (open world), B.6 (16 participation properties), B.7
(redesign prohibition). `UCL-000001`'s own disclosure names it: *"CEP-009 ADDENDUM B owns
constitutional admission."* Authoring a second contract is **refused** under `CEP-009` B.7.3,
`CMG-000001` XLIII.1 and `CMG-L-14`. Disposition **REUSE**.

The mission's disposition vocabulary maps completely onto located owners:
PASS → LXXVII.2(a) / `DR-STAGE-PASS` · REUSE → LXXVII.2(a) · EXTEND → LXXVII.2(b) ·
CREATE → LXXVII.2(c) · RECORD AS GAP → LXXVII.2(d) · REJECTED → LXXVII.2(e) ·
HOLD → LXXVII.5 · NOT_APPLICABLE → `applicability_classes` ·
SUPERSEDED → `CEP-009` IV.4 + XXIV.3 (4 recorded in `ucda-decisions.json`). **Nothing missing.**

### 4.E Identity · registry · knowledge · assurance · convergence · autonomy

| # | Mechanism | Canonical owner | Disp. |
|---|---|---|---|
| E-01 | Deterministic identity (kernel) | `engine/kernel/identity.py` — no wall-clock, no RNG | PASS |
| E-02 | Identity plane crosswalk | `UIS-001`, G-24, incl. `--check-no-identity-minting` | PASS |
| E-03 | Append-only identity ledger | `00-BOOK/DATA/id-ledger.json` (1,225 / 1,225 / 95); `CMG-000001` XXX.5 binds, forbids a second | PASS |
| E-04 | Artifact registry | `artifacts.json` (1,194) + `UMB-005` + `CMG-DLG-13` | PASS |
| E-05 | Relationship store | `relationships.json` (12,821 / 16 types) + `engine/graph`; XXXIV.5 forbids a second | PASS |
| E-06 | Knowledge-Once | `CMG` + `RA-003` + kernel `_content_unique` + `closure_engine` | **EXTEND** (Ω-P1-D-03) |
| E-07 | Nomenclature / dictionary | `UMB-004-NOMENCLATURE-ARCHITECTURE.md` + `engine/uckp/vocabulary.py` | PASS |
| E-08 | Ontology | `CMG-000001` Art XIV + `01-WORKING/ONTOLOGY-REGISTER.md` | PASS |
| E-09 | Taxonomy (Kind set OPEN) | `CMG-000001` Art XIII.2 | PASS |
| E-10 | Identity resolution engine | `engine/identity/` | **GAP** (Ω-P1-D-07) |
| F-01 | Validation | `CEP-004` + `engine/validation` + `verify.sh` | PASS |
| F-02 | Verification | `VERIFICATION-RUNBOOK.md` + `verify.sh` (6 stages, coverage ≥90%) | PASS |
| F-03 | Certification | `CEP-005` + `engine/certification` | PASS |
| F-04 | Aggregate certification | `UCCEP-000000` — 26 gates, 47 checks, 47/47 fail-closed, 1 advisory | **EXTEND** (Ω-P1-D-04) |
| F-05 | Ratification | `CEP-006` + `UCOS-URAT-001`, G-19 | PASS |
| F-06 | Freeze eligibility | `CEP-007` + `UCOS-UFEP-001`, G-21 | PASS |
| F-07 | Evidence & traceability closure | `CEP-008` + `UCOS-UTCE-001`, G-20; XXXVII.4 forbids a parallel store | PASS |
| F-08 | Audit & compliance assurance | `CEP-010` (`CMG-DLG-10`, `CMG-DLG-11`) | PASS |
| F-09 | Gap analysis | `CMG-000001` Art LXXVIII + `04-REPOSITORY-GAP-ANALYSIS.md` | **EXTEND** |
| G-01 | Determinism / hermetic double-build | `engine/determinism`, `determinism.yml` | PASS |
| G-02 | Replay | `UER-000001` + `engine/runtime/execution/replay.py` + `--render` drift gates | PASS |
| G-03 | Deterministic fixed point | `UCOS-RFP-001`, G-15 — **CERTIFIED**, 13/13 criteria, 3 passes, 35 stages | PASS |
| G-04 | Repository Truth++ | `UCCEP-000000/02-REPOSITORY-TRUTH-REGISTER.md` + `UCOS-AEE-001` loop | PASS |
| G-05 | Baseline inheritance / succession | `BASELINE-001`, G-23 | PASS |
| G-06 | Non-terminality | `UCL-S-0450` (`reenters`) + ACEE `OBL-NON-TERMINAL` | PASS |
| G-07 | Corpus currency | `UKAP-001`, `CK-CORPUS-CURRENCY` | PASS |
| G-08 | Context assimilation | `UAKOS-CLOSURE-008` + `CEP-002` Art 28 | PASS |
| H-01 | Completion invariants | `ACEE-000001` — 48, **0 unsatisfied**, G-26 OPEN | PASS |
| H-02 | Engineering goal plane | `ACEE-000001` — 6 goals, `unbound=0`, `create=0inv/0cap` | PASS |
| H-03 | Autonomous disposition rules | `acee-declaration.json:disposition_rules` (12) | PASS |
| H-04 | Obligation rules over goals | `acee-declaration.json:obligation_rules` (11) | PASS |
| H-05 | Open-world expansion axes | `CEP-009` B.5 + 16 + 23 certified axes; ACEE 30/30, UCL 32/32 | PASS |
| H-06 | No closed enumeration (self-guards) | `--check-no-enumeration` ×5 engines; kernel `QG-02` | PASS |
| H-07 | Mechanism plane | **Art LXXXII delegation** — see §2 | **PASS** (was EXTEND; withdrawn) |

### 4.F Census result

| Disposition | Count |
|---|---|
| PASS | 72 |
| REUSE | 4 |
| EXTEND | 7 distinct remediation targets |
| RECORD AS GAP | 1 |
| HOLD | 1 (this artifact's own standing, §0) |
| REJECT | 0 |
| **CREATE** | **0** |

---

## 5. Deficiency register — 7 EXTEND · 1 GAP · 0 CREATE · 1 WITHDRAWN

### Ω-P1-D-01 (WITHDRAWN) · "The mechanism plane is not open"

**Withdrawn.** The earlier finding held that `umk-kernel.json`'s 22 abstractions were a
closed enumeration because the manifest declares no `open` / `closed_enumeration` /
`admission` pointer. That inferred constitutional closure from a literal declaration and
inferred a missing central registry from the absence of a manifest — both prohibited
inversions. Corrected: openness is constitutionally declared at `CMG-000001` XIII.2, the
kernel conforms (probe: 23 → 25 by registration, no source edit), the 22 abstractions are an
Art LXXXII **recognition** crosswalked to registered meta-types (`umk_engine.py` measures
them against `registry.metatype_keys()`), and a central mechanism manifest is **prohibited**
by LXXXII.1 and `CMG-L-14`. **No extension is required. Disposition: PASS.**

### Ω-P1-D-02 · Delegation records omit a mandatory element — **EXTEND**

**Authority:** `CMG-000001` XVIII.2 — a delegation record SHALL carry *exactly* identifier,
concern, owner, basis, disposition, **revocation condition**; *"A record missing any element
IS void (`CMG-L-07`)."* **Owner:** `CMG-RET-05` retains this concern; representation is
`CMG-REGISTRY.json:concerns`; validator is `cmg_validate.py:check_concerns`.
**Conformance measured:** 60 records; the union of fields present is
`{id, concern, owner, disposition, basis, owner_paths}`; **`revocation_condition` is absent
from all 60**, and `check_concerns` does not test for it. By the literal terms of XVIII.2
every delegation record is void. **Extend:** append `revocation_condition` to the 60 records
and one check to the located validator. Data + one validator predicate; no new authority.

### Ω-P1-F-01 · Disposition vocabulary inconsistency — **RECORDED, referred**

**Authority:** XVIII.2 names the disposition set as *"(EXTEND or REUSE)"*; LXXXII.3
establishes a retained residue whose records are dispositioned `RETAIN`. **Measured:** 49
`REUSE`, 11 `RETAIN`, 0 `EXTEND`. `RETAIN` is constitutionally intended by LXXXII.3 but is
not admitted by XVIII.2's enumeration. This is an inconsistency between two clauses of the
same instrument, not an implementation defect. **Not remedied here** — referred to
`CMG-000001` under Art LXXVIII. Recorded so it is not later discovered as drift.

### Ω-P1-D-03 · Three competing admission authorities in `engine/` — **EXTEND**

**Authority:** `CMG-L-14` (X.14) *"SHALL NOT create a second registry … where one exists"*;
`CMG-INV-02`; `LXXXII.1`. **Owner:** `engine/kernel/registry.py` — the only governed and the
only XIII.2-conformant one. **Conformance measured:**

- Three implementations each self-describing as the single authority:
  `engine/kernel/registry.py::UniversalRegistry`, `engine/registry/universal/core.py::RegistryCore`,
  `engine/knowledge/ukip/registry.py::KnowledgeRegistry`.
- **Zero import edges** from `engine/registry` or `engine/knowledge` to `engine.kernel` —
  forks, not layers.
- Two error taxonomies duplicate **4** class names (`DuplicateRegistrationError`,
  `KnowledgeOnceViolation`, `RegistrationNotFoundError`, `AuditIntegrityError`).
- Two identity schemes: `UMK-<SLUG>-<12hex>` vs `UCOS-<CODE>-<12hex>`.
- **Contradictory Knowledge-Once semantics** on identical input: kernel raises
  `KnowledgeOnceViolation`; UKIP records `CORROBORATED`.
- Governance: `grep "Governance\|Constraint" engine/registry/universal/` → **zero**. Only the
  kernel's admission is governed.

Repository Truth records this against itself: ADR-0003 states the kernel realizes the open
substrate *"beneath and beside"* the existing registry — "beside" is the parallel authority.
**This is the one measured `CMG-L-14` FAIL.** **Extend:** designate `engine/kernel/` as the
single admission authority and reduce the other two to adapters, under a recorded
`UCDA-000001` decision. Create no fourth.

### Ω-P1-D-04 · No mechanism measures participation across the population — **EXTEND (PRIMARY)**

**Authority:** `CEP-009` B.6.1 — a newly introduced construct SHALL satisfy 16 properties,
eight of which are literally *"Participate in ontology / taxonomy / registries / dependency
graphs / governance / validation / certification / traceability"*; B.6.2 *"Absence of evidence
IS NOT evidence of satisfaction"*; B.6.3 measurement SHALL be fail-closed. `LXXXII.6` routes
an unowned concern to a gap under Art LXXVIII. **Owner:** `UCCEP-000000/uccep-bindings.json`
(aggregate certification) and `ACEE-000001/acee-invariant-manifest.json` (completion
invariants). **Conformance measured:**

- 26 gates, 47 checks, all fail-closed. **Zero checks assert programme-home coverage**
  (computed result: `[]`).
- 31 programme homes carry `*_engine.py`; **35 engines** exist; **17** are bound as check
  owners; **18 are not** — including `00-MASTER/UMK-000001/umk_engine.py`, the kernel's own
  binding programme.
- **4 engines are gated by nothing at all** — no aggregate check, no workflow:
  `UAKOS-PHASE-001A-R1/cert_engine.py`, `UAKOS-PHASE-001B/provenance_engine.py`,
  `UAKOS-PHASE-003R/phase3r_engine.py`, `UCOS-USIS-WAVE0/freeze_c4_engine.py`. Reachable only
  through `Makefile` targets — and **no workflow invokes `make`**.
- All 48 ACEE invariants measure *engine faculties*; none asserts *"every constitutional
  object is admitted"* or *"no unbound constitutional object exists"*.
- Every `provider_discovery` root is the programme's own home (`acee_engine.py:362`,
  `ucl_engine.py:287` — `root.iterdir()` + suffix). No engine under `00-MASTER/` walks the
  repository for constitutional object homes. `uccep_engine.py` contains no `rglob` and no
  directory walk.

**Consequence:** fail-closed against incomplete *artifacts* and against drift in
*already-bound* objects; fail-**open** against *unbound* objects. The same file's
`$gates_comment` states the principle it does not yet measure: *"A gate with no bound check
would be a MANUAL gate and is itself a finding (no manual governance)."*

**Extend, data-only, three appends to located surfaces:** (i) checks asserting every
programme home resolves to a gate and every gate to a workflow; (ii) `CK-UMK` / `CK-UMK-SELF`
bound to `umk_engine.py` plus a gate, so the aggregate certifier certifies the kernel;
(iii) participation invariants appended to `acee-invariant-manifest.json`, whose declared
admission is *"append a node record … no engine change"*. Widening the located
`provider_discovery` surfaces (also data-only) makes new homes discoverable.

### Ω-P1-D-05 · Four applicable obligations are unbound — **EXTEND (disclosed, ratcheted)**

**Measured:** `UCL-V-24` = `{"measure":"stage_obligation_failures","comparator":"<=","expect":4,"blocking":true,"bound_finding":"UCL-F-002"}`;
measured value 4. Unbound: `Gap Discovery` (OBL-VALIDATION, OBL-VERIFICATION,
OBL-CERTIFICATION — its owner `04-REPOSITORY-GAP-ANALYSIS.md` lies in a checked zone and is
executed as a pipeline member, yet no declared gate validates, verifies or certifies it) and
`Certify` (OBL-REPLAY — a pipeline member declared non-reentrant as deliberate recursion
defence).

**Assessed fairly:** this is **not** a concealed tolerance. `UCL-F-002` names all four,
traces each to a cause, and states *"The bound equals the measured total exactly, so it
carries no slack and any regression closes the gate"* — a ratchet, matching `UCL-F-001` and
`UCL-F-004`. The measurement discipline is sound; the underlying incompleteness is real.
**Extend** the gate register to bring `04-REPOSITORY-GAP-ANALYSIS.md` into certification
scope, lowering the ratchet from 4 to 1.

### Ω-P1-D-06 · Ownership matrix is narrative and drifting — **EXTEND**

**Authority:** `CMG-RET-05`; `UCL-S-0060 Canonical Owner Discovery` names the file as
`owner`. **Measured:** 52 rows plus 7 blockquote addenda that amend rows in prose rather than
in the table; §3 cites *"all 431 concepts"* while `closure.json` measures **447**; no Python
site parses the file. **Extend:** make it a derived projection of
`UAKOS-CLOSURE-002/31-CONCEPT-OWNERSHIP-REGISTER.md`, already machine-generated over 447
concepts and 11 zones.

### Ω-P1-D-07 · Kind-set implementations do not conform to XIII.2 — **EXTEND**

**Authority:** `CMG-000001` XIII.2 — the Kind set IS OPEN; admission of a Kind IS an append
operation and SHALL NOT require amendment. `kinds` is **not** among the four
constitutionally closed enumerations. LXXVI.5 — any apparent limit is *a defect*.
**Owner of open categorization:** `engine/kernel/meta.py` (`MetaTypeRef = str`).
**Conformance measured, per implementation:**

| Implementation | Behaviour on a never-seen Kind | Conformance to XIII.2 |
|---|---|---|
| `engine/kernel` | admitted by registration; open set grew 23 → 25; no source edit | **CONFORMS** |
| `engine/registry/universal/identity.py::RegistryKind` (13 members) | `RegistrationValidationError` — *"unknown registry kind … allowed=[…]"* | **NON-CONFORMANT** |
| `engine/knowledge/model.py::KnowledgeKind` (17 members) | `ValueError` — *"not a valid KnowledgeKind"* | **NON-CONFORMANT** |

`RegistryKind`'s own comment records that admitting `CONTEXT` required editing the enum and
its code map — an engine modification to admit one Kind, which XIII.2 forbids and LXXVI.5
classifies as a defect. **Extend:** derive these Kind sets from registered meta-types. The
finding is a conformance measurement against located law, not an objection to the code's
shape.

### Ω-P1-D-08 · `engine/identity/` has no source — **RECORD AS GAP** (LXXVII.2(d))

**Measured:** `engine/identity/` contains only `__pycache__` with 20 stale
`cpython-312.pyc` modules (`registry, resolver, federation, equivalence, conflicts, lineage,
provenance, trust, scoring, confidence, continuity, decisions, replay, graph, models,
evidence, contracts, errors, engine, __init__`); `import engine.identity` succeeds with
`__file__ = None`, i.e. it imports from bytecode. Separately, ownership is not a relation
anywhere in the graph: `engine/graph/model.py` has zero `owner`/`OWNS` matches and the 16
relationship types in `relationships.json` contain no `OWNS`/`OWNED-BY`. **Consequence:** a
latent identity authority exists that is unreadable, unmaintainable, unmeasured by any gate,
and importable. **Disposition:** substantive and unowned → LXXVII.2(d): record as gap under
Art LXXVIII and route to the authority competent to allocate. Restoration or deletion each
require a recorded `UCDA-000001` decision. Not decided here.

---

## 6. Fixed point — CERTIFIED

**`UCOS-RFP-001` certification obtained on the committed state at HEAD `43dec4a`:**

```
UCOS-RFP-001: REPOSITORY IS A FIXED POINT | passes=3 | stages=35 | criteria=13/13 | cycles=0 | gate=OPEN
```

All thirteen closure criteria PASS, measured over 3 full passes across 35 pipeline stages:

| Criterion | Measure |
|---|---|
| CLO-01 tree clean before the fixed point is asserted | `initial_dirty_entries=0` |
| CLO-02 every required pipeline stage exits successfully | `stage_failures=0` |
| CLO-03 no tracked file modified by any pass | `tracked_modifications=0` |
| CLO-04 no file staged by any pass | `staged_entries=0` |
| CLO-05 no untracked entry outside a constitutionally excluded location | `untracked_outside_excluded=0` |
| CLO-06 repository byte-identical after every declared pass | `non_fixed_point_passes=0` |
| CLO-07 no self-reference cycle | `cycles_detected=0` |
| CLO-08 all residue attributable to a declared producer | `unattributed_paths=0` |
| CLO-09 every discovered producer is a declared pipeline stage | `undeclared_producers=0` |
| CLO-10 every producer write lies inside a declared write zone | `producer_writes_outside_zones=0` |
| CLO-11 declared write zones pairwise disjoint (ownership exactly one) | `overlapping_write_zones=0` |
| CLO-12 no producer candidate left unprobed | `unprobed_producer_candidates=0` |
| CLO-13 no stage writes into another stage's write zone | `cross_zone_writes=0` |

Producer sweep: `candidates=94 producers=0 inert=13 unreachable=81 unprobed=0 isolated=yes`.
Passes 1–3 each reported `modified=0 staged=0 untracked=0`.

Corroborating gate runs, each leaving the tracked tree byte-identical:

| Gate | Result |
|---|---|
| `ucl_engine.py --gate` | OPEN · stages 45 · order 45, cycles 0 · runs 3/3 deterministic · criteria 41/41 · seal `50cebedc…` |
| `acee_engine.py --gate` | OPEN · invariants 48 (0 unsat) · plans 6, unbound 0 · create 0inv/0cap · criteria 69/69 · exit 25/25 · seal `9a43d93e…` |
| `umk_engine.py --gate` | `CONSTITUTIONALLY-COMPLIANT` |
| `cmg_validate.py` | 80 mandated sections · 43 artifacts · 60 concerns (49 delegated, 11 retained) · **findings 0** · `READY-PROVISIONAL` |

**Two measurement facts recorded rather than smoothed over.**

*First:* the first `--detect` attempt reported `REPOSITORY NOT A FIXED POINT — the tree is not
clean before verification`, naming **this determination artifact** as the sole cause. `CLO-01`
is fail-closed by design: *"the fixed point is a property of a COMMITTED state; commit or
restore first."* The artifact was therefore held outside the tree, the certification was taken
on the clean committed state, and the artifact was restored. **Consequence, recorded:** a
PROVISIONAL untracked determination is by construction outside the fixed point and blocks its
re-assertion until it is either admitted through the registration transaction or removed.
Re-running `--detect` while this artifact remains untracked will correctly report NOT A FIXED
POINT for that reason alone.

> **Discharged under Ω-E06-B.** The blocking condition recorded above was measured again and
> confirmed — `rfp_engine.py --gate` reported `REPOSITORY NOT A FIXED POINT — the tree is not
> clean before verification`, naming this artifact and its three companions as
> `pre-existing untracked`. The first of the two routes this paragraph names was taken:
> admission through the registration transaction. The artifact was not removed, and no
> measurement in this determination was altered to reach the fixed point.

*Second:* `cmg_validate.py` reports **0 findings** while Ω-P1-D-02 measures that all 60
delegation records omit the `revocation_condition` that XVIII.2 declares mandatory and whose
absence XVIII.2 makes the record *void*. The validator does not test for that element, so a
constitutional non-conformance passes silently. This is the mission's own thesis
demonstrated inside the delegation register: **incompleteness admitted, then discovered
later.** It raises Ω-P1-D-02 from a data gap to the sharpest available evidence that
constitutional completeness must be proven at admission rather than assumed from a green gate.

---

## 7. Determination

**Repository Truth already contains the Universal Constitutional Kernel, and it already
contains the universal pattern by which the kernel's mechanisms are owned.**

The ownership pattern is `CMG-000001` **Article LXXXII** — delegation before legislation
(`CMG-P-04`), a delegation record contract (XVIII.2), 1:1 delegation→owner cardinality,
concern→owner injectivity (`CMG-INV-02`), located-owner verification (LXXXII.4), recognition
without conferral (LXXXII.7), no parallel machinery (`CMG-L-14`), a maintenance obligation
(LXXXII.5), a retained residue (LXXXII.3) and gap routing (LXXXII.6). It is **REUSED** here
in full. No ownership topology was derived. A central mechanism registry is not missing; it
is prohibited.

The kernel is `engine/kernel/` v1.0.0, bound as derived truth by `UMK-000001`, decided in
ADR-0003, proven under six blocking quality gates, and **measured conformant** to the
constitutionally declared open Kind set of XIII.2. Its legislative ground is `CMG-000001`
(14 laws; 12 invariants; the 8-step admission procedure of Art LXXVI; the exhaustive
disposition rule of Art LXXVII; exactly four constitutionally-proven finite domains). Its
admission contract is `CEP-009` ADDENDUM B. Its lifecycle is `UCL-000001` — 45 stages
covering all 22 operations the mission names, every owner resolving. Its applicability model
is REQUIRED / CONDITIONAL / NOT_APPLICABLE with recorded evidence of non-applicability. Its
completion invariants are `ACEE-000001`'s 48, none unsatisfied.

**No constitutional CREATE is required.** For all 84 mechanisms dispositioned, a canonical
owner was located. The mission's request to *produce* a Universal Constitutional Admission
Contract resolves to **REUSE** of `CEP-009` ADDENDUM B; producing a second would create the
parallel authority the mission prohibits and is refused under B.7.3 and `CMG-L-14`.

**What remains is conformance and coverage, not capability.** Seven EXTENDs and one GAP
separate the located kernel from the guarantee the mission demands. Every EXTEND binds to an
existing owner; five are data-only appends to surfaces that already declare append-only
admission and forbid engine change.

**Answer to the exit criterion.** Repository Truth does **not** yet prove that a future
constitutional change cannot become constitutionally incomplete at admission. It proves the
opposite for artifacts (`ukb.py`, git-derived, zero-configuration, fail-closed) and for
already-bound objects (per-programme replay-drift gates). It remains fail-**open** for
unbound objects: 4 constitutional engines are gated by nothing, 18 sit outside the aggregate
determination, and no check asserts population coverage. That single gap — Ω-P1-D-04 — is the
primary finding, and `LXXXII.6` already supplies the constitutional route for it.

**Sequencing, now unblocked.** The mission's precondition — *"Do NOT continue implementing
until Repository Truth reaches deterministic fixed point"* — is **satisfied**: the fixed point
is certified at `43dec4a` (§6). Implementation of the EXTENDs is therefore constitutionally
permitted, in this order: Ω-P1-D-04 first (it makes every remaining deficiency *visible* to
the aggregate certifier), then Ω-P1-D-02, then Ω-P1-D-03 and Ω-P1-D-07 (converge admission and
categorization on the kernel), then Ω-P1-D-05 and Ω-P1-D-06, then a recorded decision on
Ω-P1-D-08 and a referral on Ω-P1-F-01. **None is performed by this determination**, which
implements nothing.

**Ω-P1 exit criteria.** Final Repository Truth verification: complete (§6, four corroborating
gates, tracked-tree drift 0). Final disposition summary: complete (§4.F — 72 PASS, 4 REUSE, 7
EXTEND, 1 GAP, 0 REJECT, **0 CREATE**). Final RFP certification: obtained — *REPOSITORY IS A
FIXED POINT, 13/13 criteria*. **Ω-P1 is closed.** Ω-P2 is not begun.

---

## 8. What this determination does NOT do

8.1 Creates no authority, registry, lifecycle, namespace, identifier or **ownership topology**
(`CMG-000001` XLI.5, `CMG-L-14`, LXXXII.1). Art LXXXII is reused, never restated.

8.2 Amends no article, clause or clause identifier of `CMG-000001`, `CEP-000`…`CEP-010`, or
any programme declaration. Renumbers nothing.

8.3 Redesigns no located owner. Every remediation is an append to, or a widening of, an
existing declared surface.

8.4 Asserts no verdict it could not measure. One earlier finding is **withdrawn** on the
record (§5, Ω-P1-D-01) rather than quietly dropped, and the fixed-point verdict was taken
only after a completed `--detect` run on a clean committed state (§6) — never asserted from
partial evidence.

8.5 Decides no open constitutional question. `CMG-OQ-01`, `CMG-OQ-02` (Tier T1 vacancy
`VAC-01`), `CMG-OQ-03`, `CMG-OQ-05`, `CMG-OQ-07` remain open; every determination resting on
them remains **PROVISIONAL**.

8.6 Implements nothing. Ω-P1 is a determination round; the EXTENDs are dispositions awaiting
the fixed point.

8.7 **Does not admit itself.** This artifact was untracked and therefore a CANDIDATE, never a
record (§0). It became Repository Truth only on acceptance of its canonical ownership and
admission through the located registration transaction — an act performed later, under Ω-E06-B,
by an authority other than this determination. The clause stands as written: no artifact here
admitted itself.

---

*END OF DETERMINATION — Ω-P1 · UNIVERSAL CONSTITUTIONAL KERNEL · AUTHORITY NONE (DERIVED
TRUTH) · STANDING ADMITTED (Ω-E06-B; issued as PROVISIONAL) · MEASURED AT `43dec4a` ·
FIXED POINT CERTIFIED 13/13 · CREATE = 0 · Ω-P1 CLOSED*
