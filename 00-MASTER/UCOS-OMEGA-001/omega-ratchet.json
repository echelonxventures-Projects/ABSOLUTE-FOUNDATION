{
  "authority": "NONE \u2014 DERIVED TRUTH. Every value here is a measurement, never a choice.",
  "best": {
    "authority_of_last_resort": 0.0,
    "declared_exemptions": 14.0,
    "import_entropy": 7.998259,
    "statement_entropy": 6.362406,
    "unexplained_exemptions": 2.0,
    "unmeasured_surface_density": 0.094795,
    "unnameable_exemptions": 49.0,
    "unreachable_artifacts": 51.0,
    "unresolved_dynamic_sites": 22.0
  },
  "corrections": [
    {
      "metric": "import_entropy",
      "reason": "Import statements per tracked artifact: 7.964238 -> 7.993862, a rise of 0.029624 across 2281 tracked artifacts. RE-SEEDED for the coverage programme that took the repository from 97.1942% to over 98% under the CD-02 floor, and the rise is that programme's cost stated as a number. Sixteen new test modules were admitted and forty-odd existing suites extended; a test module imports the surface it measures, so it imports more than the average artifact, and admitting one necessarily raises an average taken over all of them. THE METRIC IS WORKING, NOT WRONG: nothing here claims the repository became less tangled. What it records is that the tangle was bought, by whom, and for what. COMPENSATION WAS PERFORMED FIRST AND IS RECORDED. Fifty-three DUPLICATED function-local import statements were hoisted to module level across twenty-one suites - the same import repeated inside twelve test bodies is twelve statements for one edge, so removing the copies removes statements without removing a single edge, and the import graph is byte-identical either way. Two further sites were converted from importlib.import_module with a computed argument to a static import plus a sys.modules lookup, which also returned unresolved_dynamic_sites to its bound of 22. WHAT WAS DELIBERATELY NOT DONE: the residual would close by rewriting `from package.module import Name` as `from package import module` across the new suites and rewriting every reference, or by aliasing imported names to module-level assignments. The first trades readability for a counter and the second is a pure evasion of one - both are the gaming the Omega-4 note already refuses, and refusing them is why this entry exists instead. A JUSTIFICATION WOULD HAVE BEEN THE WRONG INSTRUMENT: it permits every future regression on this metric forever, and what happened here was one bounded, law-driven admission. The bound now binds at the new value, so the next module that raises it is refused exactly as this one was."
    },
    {
      "metric": "statement_entropy",
      "reason": "Surface statements per callable: 6.397527 -> 6.398330, a rise of 0.0008. RE-SEEDED at verification closure. The rise is four import statements and their statements, every one of them required by a LAW rather than chosen: (1) engine/universal_discovery/gate.py gained a main() delegation because UEC-000001 locates every engine/*/gate.py as an enforcement artifact and UEC-L-04 requires it to be invoked by its own module path -- 'nothing enforces by existing'; the alternative was raising the engines_with_no_invoker ceiling from 6 to 7, which is the move Omega-4 exists to refuse. (2) engine/tests/unit/test_certification_integrity.py imports the ratchet module because UCI's six laws are now held to directions rather than ceilings and the tests must supply the sealed state they compare against. (3) engine/tests/unit/test_verification_intelligence.py imports clear_derived_scope_cache and the registry module because the collection set is derived and its fixtures must be real git trees. COMPENSATION WAS ATTEMPTED FIRST AND IS RECORDED: three same-module import statements in surface.py were merged into one node and three function-local imports in the UVI test were hoisted to a single module-level import, recovering five nodes of the nine. Shaving further would be gaming the measurement rather than governing the repository, which is why the remainder is declared here instead of hidden. A JUSTIFICATION WOULD HAVE BEEN THE WRONG INSTRUMENT: it permits every future regression on the metric forever, and what happened here was one bounded, law-driven addition. FINAL VALUE IS THE FORMATTER'S, AND THE TENSION IS RECORDED RATHER THAN RESOLVED IN SECRET: the compensation described above merged three `from engine.universal_discovery import` statements in surface.py into one node, and ruff's import sorting (rule I001, part of the canonical lint gate) requires aliased imports to be split back into separate statements. The lint gate is authoritative for style, so the merge was reverted and the bound is sealed at the count ruff produces. The residual is two import nodes. A repository cannot hold both a formatter that splits imports and a ratchet that counts them without one of them yielding, and the ratchet is the one that yields -- silently fighting the formatter would make every future `ruff format` a governance event."
    },
    {
      "metric": "unreachable_artifacts",
      "reason": "RE-SEEDED after a MEASUREMENT DEFECT was fixed, not after a regression. The bound was 33 and it was wrong. graph.SCRIPT_INVOCATION matched any `*.py` token in an orchestration text and surface._invoking_planes matched it by BASENAME, so a bare filename appearing in PROSE credited every file sharing that basename with the make and ci planes. The Makefile carries the sentence 'invisible to an __init__.py predicate' inside a comment, and that one comment made all 88 package initialisers in the repository report themselves invoked; model.py, config.py, registry.py, ledger.py and ten more inflated the count the same way. Requiring a script invocation to be a PATH (to contain a '/') and matching it as a full path corrected the measurement: the honest value is 53, not 33. A JUSTIFICATION WOULD HAVE BEEN THE WRONG INSTRUMENT -- it would have excused every future regression on this metric forever, when what changed was what the metric MEASURES. Re-seeding is recorded here so the drop is auditable instead of silent."
    },
    {
      "metric": "declared_exemptions",
      "reason": "Declared artifacts outside the measurement denominator, each with a written owner reason: 10 -> 14. The four are engine/substrate/__init__.py, __main__.py, adapter.py and gate.py (194 statements), admitted by the same rule that already carries engine.uicm and named in the same table where uicm is named: pyproject `[tool.ucos.coverage_scope]`. THE CAUSE IS EXTERNAL AND WRITTEN DOWN: the package consumes uakp>=0.0.1, which sub-declaration records as publishing no release yet, so `python -m engine.substrate` answers FAULT by design and its own suite `importorskip`s on exactly that fact. Those lines do not run in ANY environment until the upstream substrate exists; leaving them inside the denominator would bind the repository's coverage figure to a third party's release schedule, and deleting the code would destroy a consumer written against a declared contract. WHAT CLOSES THIS ENTRY: when uakp publishes, delete the pyproject entry \u2014 the files return to the denominator, the skip-guarded suite runs, and this metric falls back to 10. A correction, not a ceiling: the bound keeps binding at 14, so the next declared exemption is refused exactly as this one was argued."
    },
    {
      "metric": "import_entropy",
      "reason": "Import statements per tracked artifact: 7.993862 -> 7.998259, a rise of 0.004397 across 2297 tracked artifacts. RE-SEEDED for the measurement-tail programme: 48 permanently-unmeasured statements in 32 files were closed \u2014 transition guards that refuse a mistyped lifecycle state, the audit journal's iterator and its severed prev_hash, the manifest reader's two malformed inputs, the blank field at the door, both host-check shapes, the leaf closure that repeats a meta-class, the health fold meeting DEGRADED for the first time, the search page whose index is empty on purpose, two __main__ bodies executed under measurement, and the two arms of the absolute-law count. Every one needed a symbol its suite had never imported, so every one is an import statement. COMPENSATION WAS PERFORMED FIRST: fourteen duplicate imports in the touched files were collapsed \u2014 a local re-import of a name the module already binds at the top is a second statement for a fact already held \u2014 and five same-module top-level blocks were merged. What remains is the conflict the statement_entropy entry already records and the same file settled the same way: ruff's import sorting (I001) splits aliased imports back into separate statements, the lint gate is authoritative for style, and the ratchet is the instrument that yields \u2014 silently fighting the formatter would make every future `ruff format` a governance event. A JUSTIFICATION would have been the wrong instrument: it permits every future rise forever, and this was one bounded, law-driven admission. The bound now binds at 7.998259, so the next module that raises it is refused exactly as this one was."
    }
  ],
  "floors": [
    {
      "metric": "unexplained_exemptions",
      "reason": "The two are engine/__init__.py (2 statements) and platform/__init__.py (15 statements, 4 imports). Both are ROOT package initialisers of trees measured per SUB-package, and that shape is what puts them outside every rule: coverage sources name engine.foundation and platform.identity, never engine or platform, so no source covers the initialiser itself. Naming the roots instead would admit engine.uicm \u2014 1,893 statements with zero test files, declared exempt with a written reason \u2014 and convert a dead-code finding into a coverage figure, which is the measurement laundering the exemption register exists to prevent. \u03a9 FOUND THESE: both were absent from coverage.xml under the enumerated denominator and no control could say so. WHAT CLOSES THIS FLOOR: retiring or testing engine.uicm, after which the roots can be measured whole and this metric reaches 0. That is an owner decision about dead code, not a coverage setting.",
      "value": 2.0
    },
    {
      "metric": "unnameable_exemptions",
      "reason": "Artifacts whose top-level root is not a Python identifier \u2014 00-MASTER, 00-BOOK and 00-CMG \u2014 so NO coverage source could name them, by the language's own rule for identifiers rather than by any policy here. They are governance engines: owned (\u03a9-2 derives an authority for every one), invoked (verify.sh stages, Makefile targets and workflows all reach them) and unmeasurable by coverage as currently laid out. WHAT CLOSES THIS FLOOR: making those engines importable under test rather than only executable as scripts, which is a real change to how they are structured and not a setting. Until then the population may fall and hold; it may never rise, because a floor accepts the stall and never a regression.",
      "value": 49.0
    },
    {
      "metric": "unreachable_artifacts",
      "reason": "Artifacts no import, orchestration plane or entry point reaches. Declared as an OPEN FINDING rather than an acceptable state, so the number is argued about instead of tolerated. NOW AT 47, AND THIS IS THE FIFTH FALL FOR ONE REASON. 52 -> 51, then 51 -> 49, then 49 -> 48, then 48 -> 47: each time the coverage programme admitted a suite that imports a module nothing had reached, and an artifact that read as dead became one a test runs. The one that fell this time is engine/execution_environment/__main__.py, the package's documented 'python -m' form: nothing in the repository imported it, so the single line binding that invocation to the gate's entry point was unmeasured and a rename would have broken the documented command with the whole suite still passing. A test now imports it and asserts the binding, which is what makes it reachable. AN EARLIER LOWERING WAS TAKEN BADLY AND THE RECORD IS KEPT. A 51 -> 50 was once recorded during the engine.compiler.partition layering correction, on a measurement taken while the working tree still held uncommitted files. Once those were committed the value returned to 51 and CI refused the seal as a REGRESSION against a best that had never existed in committed truth. UCOS-RIB-001 already states the rule that was not applied: it refuses to compute on a dirty tree because 'the fixed point is a property of a COMMITTED state'. This lowering is measured on a committed tree. NINETEEN OF THE REMAINDER ARE __init__.py FILES, still the largest single group and the most tractable: a package initialiser that nothing imports is a package nothing imports. WHAT CLOSES THIS FLOOR: deleting what is finished and wiring what is not, which is a per-file owner decision. The bound still binds in the direction that matters - one more unreachable artifact REGRESSES and is refused, because a floor excuses the stall and never a regression.",
      "value": 47.0
    }
  ],
  "holds": {
    "authority_of_last_resort": 10,
    "declared_exemptions": 10,
    "import_entropy": 0,
    "statement_entropy": 0,
    "unexplained_exemptions": 10,
    "unmeasured_surface_density": 0,
    "unnameable_exemptions": 10,
    "unreachable_artifacts": 0,
    "unresolved_dynamic_sites": 7
  },
  "how_to_read": "best[m] is the lowest value of metric m this repository has ever recorded. A run measuring more than best[m] is REFUSED unless justifications names m with a reason. A justification permits one run and never moves best[m], so a regression cannot become the new normal by being explained. holds[m] counts consecutive runs at an unchanged value; a CONVERGENT metric held at a non-zero value for 5 runs is reported STALLED, because an exemption class that stops shrinking has become a convention. floors[m] argues that a particular value IS m's floor and so accepts the stall \u2014 it does NOT excuse a regression, and it is refused as stale the moment m is measured below it.",
  "justifications": [],
  "kinds": {
    "authority_of_last_resort": "CONVERGENT",
    "declared_exemptions": "MONOTONIC",
    "import_entropy": "ENTROPY",
    "statement_entropy": "ENTROPY",
    "unexplained_exemptions": "CONVERGENT",
    "unmeasured_surface_density": "DENSITY",
    "unnameable_exemptions": "CONVERGENT",
    "unreachable_artifacts": "CONVERGENT",
    "unresolved_dynamic_sites": "MONOTONIC"
  },
  "producer": "engine/universal_discovery/ratchet.py",
  "schema": "ucos-omega-ratchet",
  "version": "1.0.0"
}
