# Output 12 — Discovery Observations

> **STATUS DOMAIN:** GOVERNANCE (measurement) · **STATUS BASIS:** observations arising from the measurements of Outputs 1–11 at HEAD `9de85ad`; each carries its own evidence and method

| Field | Value |
|---|---|
| PROGRAMME | `UCCEP-000007` · OUTPUT 12 |
| AUTHORITY | **NONE — DERIVED TRUTH** |
| SUBJECT | Measured observations that no single inventory owns. An observation is a **measurement**, not a judgement, a defect claim, or a recommendation. |
| RULE | Nothing here is repaired, resolved, or acted upon. Gaps recorded by located owners are in Output 13; risks in Output 14; open questions in Output 15. |

---

| Id | Observation | Evidence | Method | Impacted capability | Constitutional reference |
|---|---|---|---|---|---|
| **OBS-1** | `git ls-files` without `core.quotePath=false` splits zone and extension tallies on the non-ASCII `Ω∞` filenames, producing phantom zones (`"02-MASTER`, `"00-BOOK`) and a phantom extension (`md"`, 94 occurrences). All counts in Output 1 use `quotePath=false`. | `evidence/zone-counts.txt` before/after | M-1 | measurement reproducibility | — |
| **OBS-2** | `UCCEP-000001` … `UCCEP-000004` have **no directory** in the repository. The chain present is `000000`, `000005`, `000006`, and `000007` (created by this programme). Their absence is recorded; no cause is asserted. | `ls 00-MASTER/`, `git ls-files 00-MASTER/` | M-1 | programme lineage | — |
| **OBS-3** | The OA-1 commit removed exactly **3 lines from each** of `CEP-001` and `CEP-002`, and every removal is a version statement (`\| VERSION \|`, `XXV.1` / `26.3`, END-OF-ARTIFACT). This corroborates both amendment records' claim that no pre-existing clause was modified, reordered, renumbered or removed. | `git diff 527485a..1c6e750 -- 00-CEP/…` | M-1 | constitutional integrity | `CEP-002` 26.3 · `CEP-001` XXV.1 |
| **OBS-4** | Programme tokens in `artifacts.json` are truncated at 12 characters, producing pairs that read as distinct programmes: `INFRASTRUCTU` (132) alongside `INFRASTRUCTURE` (20), plus `IMPLEMENTATI`, `ARCHITECTURA`, `CERTIFICATIO`, `VALIDATIONRE`, `REPOSITORYRE`, `TRACEABILITY`, `FINALCERTIFI` and ~30 further 12-character tokens. 94 distinct categories are recorded. | `evidence/registry-metrics.txt` | M-2 | registry classification legibility | `REG-AUTO-001` (classification) |
| **OBS-5** | The Repository Intelligence Engine snapshot predates this HEAD: `UCOS-RIE-HEALTH.json` records `corpus.artifacts = 1197`, `edges = 12817`, `pages = 9574`, evidence timestamp `2026-07-26T02:49:04Z`; the registers hold **1,199 / 12,841 / 9,587**. Every RIE figure in Output 5 is reported as the engine recorded it. | `evidence/rie-metrics.txt` vs `evidence/registry-metrics.txt` | M-3 + M-2 | capability and health intelligence currency | — |
| **OBS-6** | `UCOS-RIE-HEALTH.json → code.roots` covers `engine` and `platform` only (101,499 LOC). The four band trees `data`, `service`, `application`, `infrastructure` — **128,073 LOC** measured in Output 6 §1 — are absent from its roots, so `total_tests = 3,852` and the coverage percentages describe a subset of the realized code. | `evidence/rie-metrics.txt`, `evidence/code-loc.txt` | M-3 + M-2 | coverage/health measurement scope | `STATUS-001` §2 (non-projection) |
| **OBS-7** | Recognition and registration are independent and diverge for four instruments. `STATUS-001`, `REG-AUTO-001`, `UCI-001` and `GOV-INT-001` are recognized constitutional artifacts in `CMG-REGISTRY.json` (tier T3) yet are **not** registered corpus artifacts, because `00-BOOK/CONTROL-TOWER/` is in `EXCLUDE_DIR_PREFIXES`. | `config.py:752`; absence from `artifacts.json` | M-2 | corpus vs constitutional membership | `CMG-000001` X.1, XV.6 |
| **OBS-8** | `.gitignore` line 72 excludes **`00-MASTER/**/evidence/`**. At the OA-1 commit this kept **60 evidence files** out of version control, including the nine inputs to the `UCCEP-000006` authorization seal `f497410c…` whose §9 table prescribes reproduction via `shasum -a 256 -c`. The exclusion is a declared policy, scoped by its own comment to the `00-MASTER` lane only. | `git check-ignore -v`; `.gitignore:68-72` | M-1 | evidence reproducibility from committed history | `CEP-008` (evidence operation) |
| **OBS-9** | `Depends-On` (4,774) and its declared inverse `Required-By` (4,697) differ by **77**, while all seven other type pairs are exactly balanced. | `evidence/registry-metrics.txt` | M-2 | graph inverse completeness | `CMG-000001` XIV (relationship types) |
| **OBS-10** | The graph carries **1,224 nodes** against **1,199** registered artifacts. The 25-node difference equals the volume count (25), so volume nodes participate in the graph alongside artifacts. | `engine.graph.cli validate`; `volumes.json` | M-3 + M-4 | graph node population semantics | — |
| **OBS-11** | 4,774 `Depends-On` edges arise from only **190** artifacts carrying explicit `dependencies`; the remainder are generator-derived (a sampled edge carries `note: "structural:chain"`). The derivation rule is `config.py :: CHAINS`, whose `ENG` sequence was corrected at this baseline to `ENG-GOV-001` Output 11 Option B. | `evidence/dag-computed.txt`; `config.py` diff | M-4 + M-1 | dependency provenance | `CEP-009` Art IV.3 (corrective change) |
| **OBS-12** | The branch `programme/evo-usis-005` has **no upstream configured**, so ahead/behind is not computable and nothing in the OA-1 baseline has been pushed. The repository's own operational memory previously recorded `0/0 (synced)` against `origin/governance-reconciliation`, a different branch. | `git rev-parse --abbrev-ref @{u}` → *no upstream* | M-1 | baseline durability off-machine | — |
| **OBS-13** | Registration parity is exact and idempotent at this baseline: eligible **1,199** = registered **1,199**, with 0 unregistered / unclassified / invalid / reconciled-set drift, and a full re-run of the transaction left `id-ledger.json` and `artifacts.json` byte-identical with the tree still CLEAN. | `ukb enforce --pre`; `register.sh --guard`; digests before/after | M-3 + M-4 | REG-AUTO-001 synchronization | `REG-AUTO-001` §7, §16 |
| **OBS-14** | `00-MASTER/STATE/mcs-state.json`, the declared machine mirror of `MCP-002`, records `repository.branch = governance-reconciliation`, `head = EC3-B13-U11 realize+sync`, `sync.ahead = 5`, `captured_utc = 2026-07-22`. The canonical Markdown records HEAD `1c6e750` on `programme/evo-usis-005`. `MCS-000` §02 provides that the Markdown governs on divergence. | `evidence`-free direct read of both files | M-2 | operational-memory mirror currency | `MCS-000` §02 |
| **OBS-15** | Four of the fifteen lifecycle dimensions are **BLOCKED** (`build`, `unit_testing`, `security`, `production`, `operational` — five in total) and four are **NOT_STARTED**, while every automated dimension carries `as_of = 2026-07-15`, predating this HEAD by 11 days. | `evidence/cert-ct-metrics.txt` | M-2 | control-tower signal currency | `STATUS-001` §1 (domain independence) |
| **OBS-16** | Registers 8–11 of the eleven-register set declared by `GOV-INT-001` §6.2 — `changes.json`, `knowledge.json`, `regeneration.json`, `rollback.json` — are **absent** from `00-BOOK/DATA/`. Registers 1–7 are present. | `git ls-files 00-BOOK/DATA` | M-2 | change-intelligence realization | `GOV-INT-001` §6.2, §11 |
| **OBS-17** | This programme's own directory is registration-excluded: creating `00-MASTER/UCCEP-000007/` allocated no identifier and produced no registry mutation, verified by `register.sh --guard` exit 0 with zero drift after creation. | `config.py:754`; guard run | M-3 | write-scope containment | boundary **P-5** |

---

*`UCCEP-000007` Output 12. AUTHORITY = NONE (DERIVED TRUTH). Observations are measurements. Nothing here is a defect determination, and nothing here was repaired. `CERTIFIED-PROVISIONAL`; Tier T1 VACANT.*
