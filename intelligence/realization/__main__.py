"""``python -m intelligence.realization`` — the head-less realization entry point."""

from __future__ import annotations

from intelligence.realization.cli import main

if __name__ == "__main__":
    raise SystemExit(main())
