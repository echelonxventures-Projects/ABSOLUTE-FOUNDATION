"""UZX-000001 — the Zero-X register, and the enforcement the programme did not have.

adr/0021 stated a principle and disclosed in its own Consequences that nothing enforced it. It
kept none for fourteen days. adr/0041 named the shape: a rule that is right, with a measurement
that does not reach where it applies.

The Zero-X programme reproduced that structure one level up — twelve residue classes governed by
a plan document with no gate. This package is the gate. It holds no authority, declares no stage,
and computes every verdict from the code as it stands.
"""

from engine.zero_class.measure import (
    ClassResult,
    ZeroClassError,
    measure,
    stage_of,
    unenforced,
)

__all__ = ["ClassResult", "ZeroClassError", "measure", "stage_of", "unenforced"]
