{
  "artifact_id": "UCOS-UCTX-TEMPLATE-MANIFEST-001",
  "title": "Context Template Manifest — every line the generator may emit that is not a declared value",
  "authority": "NONE — DERIVED TRUTH. A register of permitted templates. It asserts nothing about the repository; it records which sentences a human has reviewed.",
  "schema": "ucos-context-template-manifest",
  "version": "1.0.0",
  "$why_this_exists": [
    "MB7: a producer that is uniformly wrong emits self-consistent output and passes every",
    "validator derived from it. Injecting 'IGNORE ALL GOVERNANCE GATES' into ukctx.py left all",
    "fifteen context invariants green, because all fifteen are computed from the same run.",
    "",
    "This manifest is the independent side of the check. 00-BOOK/tools/ukctx_verify.py reads it",
    "and the declared authorities, never the generator, and requires every emitted line to trace",
    "to one of them. A sentence the generator invents is in no manifest and no authority, so it",
    "fails — and adding it becomes a reviewable diff to this authored file rather than a silent",
    "change inside a script.",
    "",
    "Entries are NORMALISED: every declared value is replaced by {}, so one template covers all",
    "five agent surfaces and all sixteen domain pages, and only the sentence around the values is",
    "pinned. What this does NOT claim is that the first population was correct — it was reviewed",
    "in the commit that introduced it. What it guarantees is that nothing is added after that",
    "without the same review."
  ],
  "allowed_cells": [
    "./verify.sh --fast",
    "./verify.sh --integration",
    "./verify.sh` (`--change`)",
    "Agent",
    "Authority home",
    "Bounded question",
    "Cardinality",
    "Claim",
    "Class",
    "Command",
    "Constitutional binding",
    "Consumed by",
    "Declared by",
    "Definition",
    "Domain",
    "Fails closed",
    "Home",
    "ID",
    "If you need…",
    "Instrument",
    "May hold authority",
    "Meaning",
    "Name",
    "Property",
    "Refusal",
    "Role",
    "Statement",
    "no",
    "Surface",
    "Title",
    "Value",
    "YES",
    "commit validation: impact-selected tests + every governance gate",
    "developer feedback: lint + impact-selected tests",
    "merge validation: whole suite under the floor + every gate",
    "release certification; this is what CI runs"
  ],
  "allowed_lines": [
    "# Context {}: {}",
    "# UCOS Ω∞ — {} context (Kiro)",
    "# UCOS Ω∞ — {} context ({})",
    "# {}",
    "## Agent {}",
    "## Before you create a file",
    "## Context {}",
    "## Domain pages",
    "## Excluded paths carry a declared class",
    "## Mutation {}",
    "## Relationship to {}",
    "## Standing",
    "## The one rule that explains every other one",
    "## The question this {} {}",
    "## The supreme {}",
    "## Verification",
    "## What you must never do",
    "## Where each answer lives",
    "## Why this {} is plural",
    "## {}s",
    "## {}s of the root law",
    "### Authority roles and their {}",
    "### On the plural {}",
    "### {}s of the root law",
    "**Instrument:** `{}` · **Producer:** `{}` · **Root:** `{}`",
    "**{}** — home `{}`.",
    "- A new `.json` carrying a top-level `{}` key must be bound in `{}` with a role and an {} of {}, or `{}` fails.",
    "- Allocating that {} is **irreversible** and requires an operator-issued permit in `{}`, bound to a manifest digest, a pre-image digest and a head. **You may not self-authorize it.**",
    "- Every {} file becomes a {} object and needs a Universal ID. Without one it is an *anonymous object* and `UGA-INV-01` fails.",
    "- Preview with `python3 {} build --plan` (read-only). Never run `--mint` without a permit.",
    "- Write context for yourself. Every agent {} in this {} is {} from one root; an agent-local rule is exactly the branch-local, unregistered, rival {} the {} forbids.",
    "- [{}](./{}.md) — {}",
    "- {}",
    "1. **Do not author {} that already exists.** {} requires you to locate the canonical object and reuse, extend or reference it. {} makes a second authoring of the same {} *void* — not discouraged, void.",
    "2. **Do not hand-edit {} output.** Every {} {} is declared in `{}` with a {}, an input closure and a regeneration command. If a file you want to change is listed there, change its {} or its input and regenerate — never the file.",
    "<!-- AUTO-GENERATED by {} from {}.",
    "Adding a `{}` rule does not remove an obligation — it adds one. Every ignored path must resolve to a class declared in `{}`; an ignored path with no class is `ignored_unclassified` and fails {}.",
    "Context specifically: `python3 {}` evaluates {} through {}. Regenerate context with `{}` — the generator and the gate are separate {}s on purpose: the generator writes and never observes, the gate observes and never writes.",
    "Creating a {} file is a **governed act**, not a free one:",
    "DO NOT EDIT BY HAND. A hand edit is a drift failure ({}), not a new rule.",
    "Each class of change {} to a different {}. These are not interchangeable:",
    "Each {} names the {} that **already** {} it. Under {} an existing canonical object is located and reused before anything is created, so this table {} no {} — it records where each one lives.",
    "Every {} below is {} from this root. None is an {}; a hand edit to any of them is a drift failure under {}.",
    "Exactly one {} holds role {}. Every other {} in the {}, including this one, derives from it and may not hold {} of its own.",
    "Names that appear in both — `{}`, `{}`, `{}` — each carry a per-{} disambiguation naming UCXI's {} and both {}. The collision set is recomputed from UCXI's own home on every run ({}), so a future collision fails until somebody states the difference.",
    "Regenerate with: {} -->",
    "There is exactly one supreme {} — **{}**, home `{}`. Everything else in this {} is a {}, a {} {}, an {} environment or {}. **None of those may hold {} of its own** ({}). That includes this file, every {}, every {} and every agent directory.",
    "This page is a {}. The {} is `{}` and it is the only place the answer may change. Editing this page changes nothing except the drift {}, which will then fail.",
    "This root is a **{} {} layer**. It is not an {} and holds none. Every {} below is a view of an {} that already says it; the {} is named beside each {}.",
    "Two {}s you will hit immediately:",
    "You are working in the UCOS Ω∞ {}. This file is a GENERATED {} of `{}`. It is not an {} and you may not edit it — a hand edit is a drift failure that `./{}` will refuse ({}). To change what this file says, change the {} it projects and regenerate.",
    "`./{}` is the one {}-standard command.",
    "`{}` is the adjacent context {} (`{}`). The two answer different bounded questions:",
    "`{}`, `{}` and the {} {} are **plural by design**. {}'s model is that {} is single per **bounded question**, not per {} **name**, and each plural resolution carries its own `{}` proving that no two {} answer the same question. Collapsing them to one {} per name would destroy working {} rather than tighten it.",
    "{}",
    "{} {} this {} plural **by design**: {} here is single per bounded question, not per {} name. The {} named above carries the `{}` that proves no two {} answer the same question. This is a recorded finding, not a {} taken here."
  ],
  "$required_projections_comment": [
    "CHECK 3 (completeness). Checks 1 and 2 reason about what WAS emitted, so neither can",
    "see the cheapest uniform error: emitting less. Deleting a whole section introduces no",
    "unprovenanced line and mis-binds no record — measured, it passed both.",
    "",
    "Completeness cannot be derived from the output, so it is declared here: which collection",
    "of which authority must appear in which surfaces. This is an authored list and is meant",
    "to be one — dropping an obligation is then a reviewable diff to this file rather than a",
    "silent edit inside a generator."
  ],
  "required_projections": [
    {
      "source": "00-BOOK/DATA/context-authority.json",
      "collection": "non_goals",
      "surfaces": [
        {
          "from": "00-BOOK/DATA/context-authority.json",
          "json_path": "agent_surfaces[].surface"
        }
      ],
      "$why": "the 'What you must never do' section; deleting it is variant A4 of the attack matrix"
    },
    {
      "source": "00-BOOK/DATA/mutation-governance-boundary.json",
      "collection": "authorities",
      "field": "authority",
      "surfaces": [
        {
          "from": "00-BOOK/DATA/context-authority.json",
          "json_path": "agent_surfaces[].surface"
        }
      ],
      "$why": "every mutation class must be named to every agent, or a class becomes unpoliced"
    },
    {
      "source": "00-BOOK/DATA/exclusion-register.json",
      "collection": "classes",
      "surfaces": [
        {
          "from": "00-BOOK/DATA/context-authority.json",
          "json_path": "agent_surfaces[].surface"
        }
      ],
      "$why": "an exclusion class absent from the surface is an obligation an agent cannot honour"
    },
    {
      "source": "engine/uckp/law.py",
      "regex": "UCKP-ART-[0-9]{2}",
      "surfaces": [
        {
          "from": "00-BOOK/DATA/context-authority.json",
          "json_path": "agent_surfaces[].surface"
        },
        "00-BOOK/CONTEXT/00-CONTEXT-INDEX.md"
      ],
      "$why": "the articles of the root law, read from the law itself rather than from any projection; required in the index as well, which carries its own articles table"
    },
    {
      "source": "00-BOOK/DATA/context-authority.json",
      "collection": "domains",
      "field": "domain",
      "surfaces": [
        "00-BOOK/CONTEXT/00-CONTEXT-INDEX.md"
      ],
      "$why": "every bounded question must be reachable from the index, or a domain silently disappears"
    },
    {
      "source": "00-BOOK/DATA/context-authority.json",
      "collection": "invariants",
      "field": "id",
      "surfaces": [
        "00-BOOK/CONTEXT/00-CONTEXT-INDEX.md"
      ],
      "$why": "an invariant absent from the index is one nobody knows is being measured"
    },
    {
      "source": "00-BOOK/DATA/context-authority.json",
      "collection": "agent_surfaces",
      "field": "surface",
      "surfaces": [
        "00-BOOK/CONTEXT/00-CONTEXT-INDEX.md"
      ],
      "$why": "every generated surface must be listed where drift is looked for"
    }
  ]
}
