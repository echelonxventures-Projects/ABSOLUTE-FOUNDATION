{
  "title": "UCOS Ω∞ Lifecycle Status Model",
  "description": "The standardized lifecycle states used across the UKB and the roll-up dimensions consumed by the Control Tower. This schema documents the canonical state set and the automated-signal envelope.",
  "type": "object",
  "additionalProperties": false,
  "required": ["state"],
  "properties": {
    "state": {
      "type": "string",
      "description": "Canonical lifecycle state.",
      "enum": [
        "NOT_STARTED",
        "PLANNED",
        "IN_PROGRESS",
        "BLOCKED",
        "UNDER_REVIEW",
        "APPROVED",
        "IMPLEMENTED",
        "TESTED",
        "CERTIFIED",
        "DEPLOYED",
        "PRODUCTION",
        "FROZEN",
        "SUPERSEDED",
        "RETIRED"
      ]
    },
    "aliases": {
      "type": "array",
      "description": "Legacy/equivalent status labels used by already-authored artifacts, mapped to a canonical state.",
      "items": { "type": "string", "enum": ["ACTIVE", "COMPLETE", "FINAL"] },
      "default": []
    },
    "source": {
      "type": "string",
      "description": "Origin of the status value.",
      "enum": ["MANUAL", "GITHUB", "GITHUB_ACTIONS", "JIRA", "SONARQUBE", "OWASP", "TRIVY", "PROMETHEUS", "GRAFANA", "OPENTELEMETRY", "KUBERNETES", "CLOUD"],
      "default": "MANUAL"
    },
    "as_of": {
      "type": "string",
      "description": "ISO-8601 timestamp of the status observation.",
      "format": "date-time"
    },
    "evidence": {
      "type": ["string", "null"],
      "description": "Link or reference to the evidence supporting the status.",
      "default": null
    }
  }
}
