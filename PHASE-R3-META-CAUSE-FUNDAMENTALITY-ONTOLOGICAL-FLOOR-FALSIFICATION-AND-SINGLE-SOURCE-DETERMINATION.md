# PHASE R3 — META-CAUSE FUNDAMENTALITY, ONTOLOGICAL FLOOR FALSIFICATION, AND SINGLE-SOURCE DETERMINATION

| Field | Value |
|---|---|
| AUTHORITY | **NONE (DERIVED TRUTH)** |
| BASELINE | `integration/recovery-001` @ `1e3e4ba9` — re-confirmed |
| INPUT HYPOTHESIS | Phase R2: Ontological Basis = `{META-A, META-B}`, Floor = 2 — **treated as hypothesis, not truth** |
| METHOD | DESTROY OR CONCEDE · every collapse path attempted · every failure documented |
| MODEL | `.runtime/phase-r3/r3_maps.py` → `.runtime/phase-r3/r3-maps.json`, digest `6df9d8bb693a7adca57c5383874f6b17` — **re-run produced an identical digest** |
| CLASSIFICATION | `[F]` · `[I]` · `[A]` · `[GAP]` · `[UNKNOWN]` |
| RESULT | **COLLAPSE-FAILURE · FLOOR-2-IRREDUCIBLE · no META-Ω exists in the repository** |

---

## R3.0 — THE TWO MEANINGS OF "DEEPER", SEPARATED FIRST

`[I]` This phase produced one methodological result that governs every finding below, and it is stated first because without it the collapse appears to succeed.

**A shared *concealment* is not a shared *cause*.**

`[F]` Measured: both meta-causes are unenforced by the same program. Of the 14 design laws `XLIX.5` routes to the Article L validator, **11 have zero occurrences in it** — including `CMG-L-10` (META-A's design law) **and** `CMG-L-04` (META-B's design law).

```
CMG-L-01 : 0   CMG-L-02 : 0   CMG-L-03 : 0   CMG-L-04 : 0   ← META-B design law
CMG-L-05 : 0   CMG-L-06 : 1   CMG-L-07 : 4   CMG-L-08 : 1
CMG-L-09 : 0   CMG-L-10 : 0   CMG-L-11 : 0   CMG-L-12 : 0   ← META-A design law (10), META-B consequence (12)
CMG-L-13 : 0   CMG-L-14 : 0
```

`[I]` This invites the collapse *"both meta-causes descend from validator non-enforcement."* **It is refuted by the direction of repair:**

| Act | Effect on META-A | Effect on META-B |
|---|---|---|
| Implement enforcement of the design laws in the Article L validator | **ELIMINATED** — the reconciliation is performed, divergence is reported | **ENTRENCHED** — enforcing `CMG-L-04` *detects and blocks* self-elevation, making the vacancy binding rather than removing it |

`[F]` `CMG-L-04` and `XLIV.7` are **prohibitions**. Enforcing a prohibition confirms the thing it forbids remains forbidden. `[F]` `CMG-L-10`, `CMG-INV-10` and `XV.5` are **mandates**. Enforcing a mandate discharges it.

`[I]` **One act eliminates META-A and strengthens META-B. A common cause must be removable by a single act; these are moved in opposite directions by the same act.** Validator non-enforcement is therefore the shared **epistemic** condition under which both remain unreported — not the shared **ontological** source from which both arise. `[I]` Every subsequent collapse candidate in this phase failed on this same distinction.

---

## R3.1 — COMPLETE META-CAUSE INVENTORY

### META-A-COMPLETE-MAP

| Field | Value |
|---|---|
| **Identifier** | META-A — the unrealized reconciliation mandate |
| **Statement** | `[F]` A derived representation that disagrees with its source is governed by the source and SHALL be regenerated, recomputed or corrected. Mandated at invariant rank; realized in the meta-constitutional validator nowhere. |
| **Origin artifact** | `[F]` `CMG-000001` — but **not exclusively**: `CEP-008` and `CEP-004` declare the same obligation independently (see below). `[I]` This **discharges `UNK-R2-01`**: further reconciliation clauses do exist outside `CMG-000001`, so `A-R2-01` was false. |
| **First appearance (deepest rank)** | `[F]` `X.10` **`CMG-L-10` (Evidence Binding)** — *"Every meta-constitutional assertion SHALL be bound to evidence under CEP-008 and SHALL be recomputable from repository state alone."* Design-law rank; precedes the invariant that encodes it. |
| **Owning artifact** | `[F]` `CMG-000001` for the meta layer (`XLIX.5`); `CEP-008` for evidence and traceability; `CEP-004` for validation |
| **Violation type** | **UNSOUNDNESS** — the machine affirms what the constitution denies |
| **Deontic operator** | **`SHALL` (mandate)** — unrealized |
| **Modal status** | **CONTINGENT** — `[F]` `XV.5` asserts achievability (*"SHALL be regenerable from repository state alone by the validator of Article L"*), and the discipline **is achieved elsewhere at HEAD** (§R3.2) |

**`[F]` Governing clauses — 18 located, across 4 instruments**

| Instrument | Clauses | Content |
|---|---|---|
| `CMG-000001` | `X.10` (`CMG-L-10`) | recomputable from repository state alone — **design law** |
| | `XI.1` (`CMG-INV-01`) | registry membership = artifacts cited as authority anywhere in the corpus — **invariant** |
| | `XI.10` (`CMG-INV-10`) | every assertion recomputable; **verification = validator exit status zero** — **invariant** |
| | `II.4` | derived vs canonical → canonical governs, derived regenerated |
| | `XII.6` | *"Derived truth SHALL always be reconciled against repository reality"* |
| | `XV.3` | projection vs input → input governs |
| | `XV.5` | Registry regenerable, else **IS invalid** |
| | `XXV.3` | located model governs; mapping corrected by amendment |
| | `XXVII.5` | declaration reconciled against registries; irreconcilable → **a finding, not a guess** |
| | `LIII.3` | evidence recomputable, not narrative; non-recomputable → judgement with **named accountable authority** |
| | `LX.2` | generated output reconciled against constitutional input |
| | `LXXIV.6` | restored derived store disagreeing with sources IS a **drift defect** |
| | `LXXXIII.9` | every assertion recomputable from repository state alone |
| `CEP-008` | `XVI.3` | registries append-only, content-addressed, **reconciled against repository truth at boot** |
| | `XXI.1` | on every boot, reconcile against repository truth; **repository truth SHALL prevail on divergence** |
| `CEP-004` | `X.1` | determinism validation — reproducible outputs **regenerate byte-identically** |
| | `XV.3` | validation checkpoints reconciled against repository truth at boot |
| `CEP-001` | `Art. XXI` | the general Recovery Model, consumed by reference by `CEP-008` `XXI.1` |

**`[F]` Broad sweep (reproducible, determination documents excluded so the sweep measures the corpus and not its own reports):** 3,205 clause-lines across 1,159 files repository-wide; **1,710 lines across 679 constitutional-instrument files.** `[I]` The sweep over-captures and is reported raw, never as an item count.

| Dependent class | Located content |
|---|---|
| **Dependent clauses** | the 18 above are mutually reinforcing; none is entailed by another (each survives the others' repeal — §R3.3 C3) |
| **Dependent projections** | `[F]` `CMG-REGISTRY.json` — **19 collections, 5 text-bound, 14 unbound** (independently reproduced) |
| **Dependent validators** | `[F]` `00-CMG/tools/cmg_validate.py` — 733 lines; **no regeneration path**; `regenerat*` = 0; `REGISTRY_RELPATH` read 3× (36, 85, 87), never written; sole write is `--emit` evidence at 725; **no corpus walk** (`os.walk`/`rglob`/`glob(` = 0) |
| **Dependent gates** | `[F]` `verify.sh:410` → `00-CMG/tools/cmg-gate.sh`, labelled *"meta-constitutional conformance (CMG-INV-01..12)"*; realization covers **10 of 12** |
| **Dependent concerns** | `[F]` `XLIX.5` — one concern, *meta-constitutional compliance*, covering `IV.1` + `CMG-L-01…14` + `CMG-INV-01…12`; **11 of 14 design laws and 2 of 12 invariants unrealized** |
| **Dependent findings** | `[F]` R3, R4, R6, R7, R8 of the Phase R1 basis; `CE-01`, `SOUND-01/02`, `FP-01…05`, `DIV-*`, `BS-01…12`; **63 of 100 live blocking dependents** |
| **Dependent gaps** | `GAP-R2-02`, `GAP-R2-04`, `GAP-R2-05`, `GAP-R2-06`, `GAP-R2-07`, `GAP-P-01`, `GAP-P-02`, `GAP-P-03` |
| **Dependent assumptions** | `A-R2-01` (**refuted here**), `A-R2-02`, `A-R2-06`, `A-R2-07` |
| **Dependent unknowns** | `UNK-R2-01` (**discharged here — YES, clauses exist outside `CMG-000001`**), `UNK-R2-02`, `UNK-R2-06`, `UNK-R2-07` (**partially discharged — 11 of 14 measured**) |

### META-B-COMPLETE-MAP

| Field | Value |
|---|---|
| **Identifier** | META-B — `ROOT-Ω` |
| **Statement** | `[F]` A corpus cannot confer on itself the standing it lacks. |
| **Origin artifact** | `[F]` **`CEP-000` §5.4** — *"Program Authority SHALL NEVER self-elevate. The Program SHALL NOT grant itself authority over constitutional content, and no CEP agent SHALL grant itself ratification authority."* |
| **First appearance (deepest rank)** | `[F]` `CEP-000` §5.4, and `CMG-000001` `VI.4` records the lineage explicitly: *"This principle **is already law** for Program Authority at CEP-000 §5.4 and is **generalized here** to all constitutional artifacts as CMG-L-04."* `[I]` The corpus itself documents META-B's descent — from Program scope to corpus scope. |
| **Owning artifact** | `[F]` **none located** — the competent authority is the referent of `VAC-01`, `located: false` |
| **Violation type** | **INCOMPLETENESS** — the constitution cannot obtain what it requires |
| **Deontic operator** | **`SHALL NOT` / `IS PROHIBITED` (prohibition)** — in force and honored |
| **Modal status** | **NECESSARY** — `[F]` *"Derivation is therefore logically foreclosed"*; *"a **missing seed**, not a **contradiction**"* |

**`[F]` Governing clauses — 18 located, across ≥6 instruments**

| Instrument | Clauses |
|---|---|
| `CEP-000` | `§5.4` (never self-elevate) · `§5.5` (tier order, T1 above the Program) · `§6.4` (SHALL NOT override any authority residing outside the Program) |
| `CMG-000001` | `VI.4` (non-self-elevating) · `IX.16` (`CMG-P-16`) · `X.4` (`CMG-L-04`) · `X.12` (`CMG-L-12` provisional standing) · `XII.6` (derived truth never a source of authority) · `XVII.4` (vacancy handling; T1 **IS** VACANT) · `XLIV.7` (**self-ratification IS PROHIBITED; no authority SHALL grant itself ratification competence**) · `LXXXI.6` (SHALL NOT self-elevate; standing **IS PROVISIONAL**) · `LXXXIII.3` (no artifact the sole source of its own superiority) |
| `CEP-006` | `I.4` (finality reserved to an out-of-corpus authority) · `II.4` (ratification outside authorization IS void) |
| external | `AUTH-03` · `AUTH-04` · `AUTH-06` (*never fabricate, assume, or simulate authority*) · `Ω-010` (*Ratification Before Structural Change*) · `CM-007` |
| corpus-wide | `CF-C4` — *"Self-certification is void. A programme may not certify itself"* — recorded as **a universal constraint** |

**`[F]` Broad sweep:** 247 clause-lines across 161 files; **185 lines across 110 constitutional-instrument files.** `[F]` `AUTH-06` alone appears in **258 files**.

| Dependent class | Located content |
|---|---|
| **Dependent clauses** | `XVII.4` closure procedure (a)–(d); `LXXX.4` ceiling; `CMG-L-12` provisional standing |
| **Dependent projections** | `[F]` `CMG-REGISTRY.json → vacancies[VAC-01]`, `located: false`; `artifacts[*].superiors = ["VAC-01"]`; `CMG-000001` `state: DECLARED` (sole `DECLARED` of 44) |
| **Dependent validators** | `[F]` `check_superiors` ✔ (resolves `VAC-01` as a recorded vacancy — **passes by design**); `readiness()` → `READY-PROVISIONAL` |
| **Dependent gates** | `cmg-gate.sh` (records, does not block); `uccep-gate` (`UCCEP-F-004` ceiling) |
| **Dependent concerns** | T1 Constitutional Authority — **no located owner** |
| **Dependent findings** | `VAC-01`, `CMG-GAP-04`, `CMG-OQ-01/02/03/07`, `DEF-02`, `UCCEP-F-004`, `UCAF-RC-01/02/03`, 5 `URAT` records at PROVISIONAL, `B-10…B-13`, `UD-01…UD-06` — **37 of 100 live blocking dependents** |
| **Dependent gaps** | `CMG-GAP-04`; `GAP-O-01` (referral addressed to a projection that can dispose nothing) |
| **Dependent assumptions** | `A-R1-02`; `A-R2-03` |
| **Dependent unknowns** | `UNK-R1-02`; `UNK-R2-04` |

`[F]` **Both maps are multiply and independently grounded — 18 located clauses each. Neither rests on a single removable clause.** `[I]` This is the first structural obstacle to collapse: a common ancestor would have to generate two 18-clause bodies of law, and no located clause has that reach.

---

## R3.2 — BIDIRECTIONAL ENTAILMENT TEST

### ENTAILMENT-A-TO-B — **`[F]` FALSIFIED**

**Method.** Grant META-A fully resolved: every one of the 18 reconciliation clauses implemented; the validator regenerates the Registry, walks the corpus, compares, and reports every divergence.

`[F]` **Surviving state:** `VAC-01.located` remains `false`. `XLIV.7` remains in force. `CEP-000` §5.4 remains in force. `LXXXIII.3`, `CMG-L-04`, `IX.16`, `VI.4`, `LXXXI.6` remain in force. `AUTH-06` remains in force across 258 files.

`[I]` **A perfect reconciliation engine can only report that T1 is vacant. It cannot occupy T1** — occupying it is exactly what `XLIV.7` prohibits the corpus from doing for itself. `[I]` Resolving META-A makes META-B **more precisely measured and no less binding.** META-B survives. ∎

### ENTAILMENT-B-TO-A — **`[F]` FALSIFIED, AND FALSIFIED CONSTRUCTIVELY**

**Method.** Grant META-B fully resolved: an external authority occupies T1 and confers finality on everything.

`[F]` **Surviving state:** `cmg_validate.py` still contains no regeneration path, `XV.5` still has 0 occurrences, `CMG-INV-01` and `CMG-INV-10` still have 0 occurrences, 14 of 19 collections remain unbound, `L.3` still names no comparison duty. `[I]` Ratification does not write code. META-A survives.

**`[F]` And the stronger form — META-A's remedy is not merely possible in principle; it is IMPLEMENTED AND OPERATING AT HEAD, under the same vacancy.**

| Located mechanism | What it does | Status |
|---|---|---|
| `00-BOOK/tools/register.sh --guard` | *"additionally fails (exit 3) if, after registration, the generated DATA/ REGISTRIES/CONTROL-TOWER/PORTAL **differ from what is committed**"* — regenerate, compare, fail on divergence | **operating**; installable as a git pre-commit hook |
| `.github/workflows/determinism.yml` | *"Reproducibility (**double-build · fail-on-divergence**)"* — `double_build(...)`, compares byte-for-byte, *"exits non-zero on ANY divergence"* | **operating** |
| `ukbx.py twin --check` | digital-twin certification — `register.sh` Phase 7/10 and Observe 4/4 | **operating** |
| `cmg_validate.py` | implements `CMG-INV-09` and `CMG-INV-12`, **which `L.3` never mandates** | **operating** |

`[I]` **Four consequences, each fatal to B ⇒ A:**
1. The regenerate-and-compare discipline whose absence *constitutes* META-A **exists, runs, and passes in this repository** while T1 is VACANT. A cause cannot be entailed by a condition under which its remedy is already demonstrated.
2. `cmg_validate.py` already exceeds `L.3` by two invariants. **Exceeding `L.3` therefore requires no amendment and no ratification** — the corpus demonstrates the permission by exercising it.
3. `CMG-INV-01` and `CMG-INV-10` are **already inside** the closed criteria set `L.2` declares exhaustive. Implementing them adds no criterion; it realizes an existing one. **No amendment path is engaged, so `XLII.5`'s *"full ratification path"* is never reached.**
4. `[F]` `LIII.3`'s fallback for a non-recomputable assertion is *"a judgement with a **named accountable authority**"*. That authority need not be T1: `CMG-INV-03` (no orphan concern, no ownerless owner) is **total over 61 concerns and passes**, so a named accountable authority is locatable in-corpus. **META-A's lawful disposal route does not pass through META-B either.** ∎

**`[I]` The scope inversion, stated because it is the sharpest located evidence.** `register.sh --guard` reconciles exactly `00-BOOK/DATA`, `00-BOOK/REGISTRIES`, `00-BOOK/CONTROL-TOWER`, `00-BOOK/PORTAL` — and `EXCLUDE_DIR_PREFIXES` excludes **exactly those directories**, plus `00-MASTER/`, from the constitutional registration universe. `[I]` **The corpus applies regenerate-and-compare to what it treats as generated, and withholds it from what it treats as constitutional.** META-A is not an absent capability. It is a present capability aimed away from the constitution.

| Output | Verdict |
|---|---|
| **ENTAILMENT-A-TO-B** | **FALSIFIED** — META-B survives META-A's total resolution |
| **ENTAILMENT-B-TO-A** | **FALSIFIED** — META-A survives META-B's total resolution, **and its remedy is demonstrated at HEAD under the vacancy** |

---

## R3.3 — COMMON-ANCESTOR SEARCH

`[F]` Ten candidate classes, every candidate tested in both directions. **Acceptance required: the candidate must generate the cause, and the cause must not survive the candidate's repeal.**

### COMMON-ANCESTOR-MATRIX

| # | Candidate class | Located candidate | Generates META-A? | Generates META-B? | Verdict |
|---|---|---|---|---|---|
| C1 | invariant failure | `XI.13` — invariant set **IS CLOSED**, *"the immutable ground against which every other part is checked"* | **NO** — closure of the set does not omit `-01`/`-10` from `L.3`; both are *in* the set | **NO** — no invariant confers standing | **REJECTED** |
| C2 | authority assumption | `X.4` `CMG-L-04` / `VI.4` / `IX.16` / `LXXXIII.3` — non-self-elevation | **NO** — scope is *"superiority, authority, jurisdiction"*; says nothing of verification | **YES** | **REJECTED — one-sided** |
| C3 | ontology assumption | **`XII.6`** — *"Derived truth SHALL always be reconciled against repository reality **and** SHALL never be treated as a source of authority"* — **the strongest candidate: both doctrines in one sentence** | partially | partially | **REJECTED — see proof below** |
| C4 | validation assumption | `L.2` — criteria *"`CMG-INV-01`…`12` **and nothing else**"*; `L.3(a)–(l)` | **YES** (realization omission) | **NO** — enforcing it entrenches META-B (§R3.0) | **REJECTED — one-sided, opposite-signed** |
| C5 | projection assumption | `XV.3` / `XV.5` / `II.4` — projection reconciled to input | **YES** | **NO** — projections confer no standing | **REJECTED — one-sided** |
| C6 | registry assumption | `X.1` `CMG-L-01` / `XI.1` `CMG-INV-01` — recognition totality | **YES** (this is R6 at invariant rank) | **NO** — recognition is not conferral; `CMG-L-07` says delegation *"SHALL create no authority"* | **REJECTED — one-sided** |
| C7 | traceability assumption | `X.10` `CMG-L-10` / `LIII.3` / `CEP-008` `XVI.3`, `XXI.1` | **YES** | **NO** | **REJECTED — one-sided** |
| C8 | completeness assumption | `LXXVII.1` — governs unknown concepts *"not by anticipating them"*; `I.5` — never closed | **NO** — openness of concept-space does not omit a check | **NO** — openness does not withhold standing | **REJECTED — generates neither** |
| C9 | governance assumption | `XLIX.5` — one concern routing `IV.1` + `CMG-L-01…14` + `CMG-INV-01…12` to the Article L validator | **YES** | **NO** — same opposite-sign refutation as C4 | **REJECTED — one-sided** |
| C10 | constitutional assumption (unlocated) | *"no entity may be the ground of itself"* | YES | YES | **REJECTED — NOT LOCATED, and contradicted by located law (below)** |

### `[F]` C3 REJECTION PROOF — co-location in one sentence is not common ancestry

`XII.6` is the only located clause containing both doctrines. It fails both acceptance tests.

1. `[F]` **META-B survives `XII.6`'s repeal.** `XLIV.7`, `CEP-000` §5.4, `LXXXIII.3`, `X.4`, `IX.16`, `VI.4`, `LXXXI.6`, `CEP-006` `II.4`, `AUTH-06` each independently prohibit self-conferral. `XII.6` is **not necessary** for META-B.
2. `[F]` **META-A survives `XII.6`'s repeal.** `X.10`, `XI.1`, `XI.10`, `XV.5`, `LIII.3`, `LXXXIII.9`, `II.4`, `XV.3`, `XXV.3`, `XXVII.5`, `LX.2`, `LXXIV.6`, `CEP-008` `XVI.3`/`XXI.1`, `CEP-004` `X.1`/`XV.3` each independently declare the obligation. `XII.6` is **not necessary** for META-A.
3. `[F]` **`XII.6`'s scope is narrower than META-B.** Its second conjunct binds *derived truth* — `DECLARATIVE` standing. `CMG-000001` holds standing **META** at tier **T1M**, not `DECLARATIVE`. `XLIV.7` binds *"no authority"* without qualification. `XII.6` cannot generate a prohibition broader than itself.
4. `[F]` **`XII.6`'s two conjuncts carry opposite deontic operators** — *"SHALL always be reconciled"* (mandate) and *"SHALL never be treated as a source of authority"* (prohibition). `[I]` A sentence may conjoin a mandate and a prohibition; **a generating cause cannot be one.** ∎

### `[F]` C10 REJECTION PROOF — "no self-grounding" is refuted by the corpus's own vocabulary

`[I]` The intuitive META-Ω is *"self-reference cannot ground."* It is unlocated, and worse, **the corpus takes the opposite position on one of the two branches.**

| Branch | Corpus's stance | Located evidence |
|---|---|---|
| self-**authority** | **PROHIBITED / VOID** | `XLIV.7` · `CEP-000` §5.4 · `X.4` · `IX.16` · `VI.4` · `LXXXIII.3` · `LXXXI.6` · `CF-C4` *"Self-certification is void"* · `AUTH-06` |
| self-**verification** | **MANDATED AS A VIRTUE** | `X.10` · `XI.10` · `XV.5` · `LIII.3` · `LXXXIII.9`; and *"self-verifying"* / *"self-certifying"* used **54×** across the corpus **approvingly** — of sealed, content-addressed, hash-chained, recomputable evidence: *"a deterministic id is recomputable and therefore self-certifying"* |

`[F]` Measured in `CMG-000001`: `self-elevation` ×6, `self-ratification` ×1, `self-constituting` ×1 — and **zero** occurrences of `self-validation`, `self-verification`, or `self-certification`. `[I]` The meta constitution prohibits self-*authority* and does not prohibit self-*verification* anywhere.

`[I]` **Recomputability IS self-grounding of truth, and the corpus mandates it.** A META-Ω of the form *"self-grounding is void"* would therefore **void `CMG-L-10`, `CMG-INV-10`, `XV.5`, `LIII.3` and `LXXXIII.9`** — five located mandates — to explain META-A. It does not unify the basis; it contradicts half of it. **REJECTED, on the prohibition against inventing what the repository does not contain, and independently on contradiction with located law.** ∎

`[F]` **Result: 10 candidate classes, 0 common ancestors. Eight are one-sided; one generates neither; one is unlocated and contradicts located law.**

---

## R3.4 — ONTOLOGICAL COLLAPSE ATTEMPT

**Attempted:** `{META-A, META-B}` → `{META-Ω}`

### **`[F]` COLLAPSE-FAILURE**

`[F]` Acceptance required all four conditions. **Three fail.**

| Condition | Result |
|---|---|
| META-A derivable from META-Ω | `[F]` **FAILS** — no located candidate derives it except one-sided realization clauses (C4–C7, C9), none of which touches standing |
| META-B derivable from META-Ω | `[F]` **FAILS** — no located candidate derives it except one-sided authority clauses (C2), none of which touches verification |
| META-Ω has smaller explanatory complexity | `[F]` **FAILS** — the only candidate spanning both (`XII.6`) is **necessary for neither** (§R3.3 C3.1–C3.2); replacing 2 multiply-grounded causes with 1 non-necessary clause **loses** explanatory content over 36 located clauses |
| META-Ω introduces no new assumptions | `[F]` **FAILS for C10** — requires an unlocated principle that voids 5 located mandates |

### `[F]` The three independent proofs of non-collapse

**Proof 1 — DEONTIC SIGN.** META-A is an unrealized **mandate** (`SHALL`, 18 clauses, 0 realizations of the two decisive invariants). META-B is an in-force **prohibition** (`SHALL NOT` / `IS PROHIBITED`, 18 clauses, honored). `[I]` A single source would have to be simultaneously the mandate and the prohibition of the same act-type. `[F]` `XII.6` demonstrates the corpus can *conjoin* them in one sentence and that the conjuncts remain independently grounded. ∎

**Proof 2 — MODALITY.** `[F]` META-A is **contingent**: `XV.5` asserts its achievability, and `register.sh --guard`, `determinism.yml` and `ukbx twin --check` achieve it at HEAD. `[F]` META-B is **necessary**: *"Derivation is therefore logically foreclosed"*; *"a missing seed, not a contradiction"*. `[I]` A contingent realization gap and a necessary derivation foreclosure cannot share a generating cause: the first is false-but-possible, the second impossible-in-principle. Whatever generates a necessity cannot generate a contingency, and vice versa. ∎

**Proof 3 — DIRECTION OF REPAIR.** `[F]` §R3.0: enforcing the design laws **eliminates** META-A and **entrenches** META-B. `[I]` A common cause is removable by one act. These respond to the same act with opposite sign. ∎

`[I]` **Proof 3 is the one that defeats the most attractive candidate**, because validator non-enforcement genuinely does conceal both. Concealment is shared; causation is not.

---

## R3.5 — MINIMUM BASIS EXHAUSTION

### MINIMUM-BASIS-PROOF

| Size | Generating sets | Basis |
|---:|---:|---|
| **1** | **0** | — |
| **2** | **1** | **`{META-A, META-B}`** |
| 3+ | ≥1, all non-minimal | any superset; each adds a member entailed by an existing one, contributing no explanatory power |

**`[F]` No size-1 basis exists. Proof:**

1. `[F]` A size-1 basis `{X}` requires `X ⟹ META-A` and `X ⟹ META-B`.
2. `[F]` Every located candidate for `X` was enumerated in §R3.3 over ten classes and tested in both directions. **Zero generate both.**
3. `[F]` `X` cannot be META-A, because `META-A ⇏ META-B` (§R3.2, ENTAILMENT-A-TO-B FALSIFIED).
4. `[F]` `X` cannot be META-B, because `META-B ⇏ META-A` (§R3.2, ENTAILMENT-B-TO-A FALSIFIED **constructively** — the remedy operates at HEAD under the vacancy).
5. `[F]` `X` cannot be a third located clause: the only clause containing both doctrines (`XII.6`) is necessary for neither (§R3.3 C3).
6. `[F]` `X` cannot be an unlocated principle: `AUTHORITY = NONE` and the invent-nothing rule forbid it, and the one available formulation contradicts five located mandates (§R3.3 C10).
7. `[I]` Therefore no `X` exists in the repository, and the minimum is **2**. ∎

`[I]` **Non-redundancy at 2:** each member carries live blocking dependents the other cannot explain — META-A **63**, META-B **37**, of 100. Removing either leaves ≥37 dependents unexplained.

---

## R3.6 — COUNTERFACTUAL EXECUTION

### COUNTERFACTUAL-MATRIX

| | **WORLD-A** ¬A, B | **WORLD-B** A, ¬B | **WORLD-C** A, B *(= HEAD)* | **WORLD-D** ¬A, ¬B |
|---|---|---|---|---|
| **Repository state** | Registry regenerable and regenerated; 19 of 19 collections text-bound; 44 of 44 artifacts in the registration universe | Registry still unregenerable; 14 of 19 unbound; 21 of 44 unhashed; `00-MASTER/`, `00-BOOK/CONTROL-TOWER/` still excluded | `[F]` 14 of 19 unbound; `EXCLUDE_DIR_PREFIXES` active; `CMG-000001` `state: DECLARED` | fully reconciled and fully ratified |
| **Blocker state** | `[I]` **37 remain** — `VAC-01`, `CMG-GAP-04`, `CMG-OQ-01/02/03/07`, `DEF-02`, `UCCEP-F-004`, `UCAF-RC-01/02/03`, 5 `URAT`, `B-10…13`, `UD-01…06` | `[I]` **63 remain** — R3, R4, R6, R7, R8 and their dependents | `[F]` **100 live blocking dependents** | 0 |
| **Validator state** | regenerates, walks the corpus, compares; reports divergence and **reports the vacancy precisely** | `[F]` unchanged from HEAD: 0 findings, exit 0, no regeneration, no corpus walk | `[F]` `findings: 0`, `EXIT=0`, `READY-PROVISIONAL` | complete and sound |
| **Governance state** | T1 **still VACANT**; `XLIV.7` still forecloses in-corpus closure; ceiling `CERTIFIED-PROVISIONAL` holds | T1 **occupied**; finality conferrable; `CEP-006` `I.4` antecedent no longer triggers | `[F]` T1 VACANT; `CMG-L-12` applies corpus-wide; 32 of 44 artifacts `PROVISIONAL` | ratified and sound |
| **Contradiction state** | R7 **resolved** (regeneration reconciles `tier: T4` against role `SUPREME`) | `[F]` R7 **persists** — `CAA-INV-01` still passes over `tier: T4` vs `SUPREME`; `XV.5` still unimplemented ⇒ **Registry still invalid by its own terms** | `[F]` R7 live; Registry invalid by `XV.5`; gate green | none |
| **Verdict** | `[F]` **META-B survives alone** ⇒ ¬(A ⇒ B) | `[F]` **META-A survives alone** ⇒ ¬(B ⇒ A) | `[F]` both live and mutually non-derivable | both absent — unreachable in-corpus, since `WORLD-D` requires the external act |

`[F]` **WORLD-A and WORLD-B are both non-empty. That is the falsification of any size-1 basis, stated as state rather than as argument.**

`[I]` **WORLD-B is partially instantiated at HEAD already** — the reconciliation discipline runs on the generated layer (`register.sh --guard`, `determinism.yml`) while the vacancy stands. `[I]` The corpus contains a working sample of the world in which META-A is solved and META-B is not, restricted to the directories the constitutional universe excludes.

---

## R3.7 — FLOOR FALSIFICATION

**Hypothesis under falsification: *"Floor = 2."***

### `[F]` FLOOR-2-IRREDUCIBLE

**`[F]` Every attempted reduction path, and why each failed:**

| # | Path attempted | Failure |
|---|---|---|
| 1 | META-B absorbs META-A (standing failure explains verification failure) | `[F]` FALSIFIED constructively — the remedy operates at HEAD under the vacancy; `L.2` already contains `-01`/`-10`, so no amendment or ratification is engaged; `LIII.3`'s fallback authority is locatable in-corpus |
| 2 | META-A absorbs META-B (verification failure explains standing failure) | `[F]` FALSIFIED — a reconciliation engine can only *report* the vacancy; occupying T1 is what `XLIV.7` forbids the corpus to do for itself |
| 3 | `XII.6` as common ancestor (both doctrines, one sentence) | `[F]` FAILED — necessary for neither; scope narrower than META-B; conjuncts opposite-signed |
| 4 | Validator non-enforcement as common ancestor (`L.2`/`L.3`/`XLIX.5`; 11 of 14 design laws unrealized, **including both meta-causes'**) | `[F]` FAILED — enforcement **eliminates** META-A and **entrenches** META-B; shared concealment, not shared cause |
| 5 | `CMG-L-04` / non-self-elevation as common ancestor | `[F]` FAILED — scope is superiority, authority, jurisdiction; verification not covered |
| 6 | `CMG-L-10` / recomputability as common ancestor | `[F]` FAILED — scope is evidence and assertions; ratification competence not covered |
| 7 | `XI.13` closed invariant set as common ancestor | `[F]` FAILED — both invariants are *inside* the set; closure explains neither omission nor vacancy |
| 8 | `LXXVII.1` / `I.5` openness as common ancestor | `[F]` FAILED — generates neither |
| 9 | Unlocated *"no self-grounding"* | `[F]` FAILED — not located; and voids five located mandates (`X.10`, `XI.10`, `XV.5`, `LIII.3`, `LXXXIII.9`) to explain META-A |
| 10 | Size-1 basis by exhaustion over all ten candidate classes | `[F]` FAILED — 0 of 10 generate both |

**`[F]` Successful reduction paths: NONE. Floor stands at 2.**

`[I]` The floor is not 2 by failure of imagination. It is 2 because the corpus contains **two categorially different kinds of law** — mandates it has not realized, and prohibitions it cannot escape — and the basis has one member of each kind.

---

## R3.8 — TERMINAL DETERMINATION

| # | Question | Answer |
|---|---|---|
| **Q1** | **Is META-A fundamental?** | **`[F]` YES — within the repository, and CONDITIONALLY.** It is not derived from META-B or from any of the ten candidate classes. `[I]` But it is fundamental as a **contingent realization gap**, not as a necessity: `XV.5` asserts its achievability and three located mechanisms achieve it at HEAD. **It is irreducible, not immovable.** |
| **Q2** | **Is META-B fundamental?** | **`[F]` YES — unconditionally.** Doubly proved from disjoint clause sets `{XLIV.7, CEP-000 §5.4, XVII.4}` and `{AUTH-03/04/06, Ω-010, CM-007}`, 18 located clauses across ≥6 instruments, `AUTH-06` in 258 files, and *"logically foreclosed"* by the corpus's own reasoning. It survives every counterfactual. |
| **Q3** | **Does META-A entail META-B?** | **`[F]` NO — FALSIFIED.** WORLD-A is non-empty: total reconciliation leaves T1 VACANT and `XLIV.7` in force. A reconciliation engine reports the vacancy; it cannot fill it. |
| **Q4** | **Does META-B entail META-A?** | **`[F]` NO — FALSIFIED, CONSTRUCTIVELY.** WORLD-B is non-empty **and partially instantiated at HEAD**: `register.sh --guard` (exit 3 on divergence), `determinism.yml` (double-build, fail-on-divergence) and `ukbx twin --check` all perform regenerate-and-compare while T1 is VACANT. `cmg_validate.py` already implements two invariants `L.3` never mandates, proving no amendment is needed to exceed `L.3`. |
| **Q5** | **Does a deeper source exist?** | **`[F]` NO — not in this repository.** Ten candidate classes tested bidirectionally; 0 common ancestors. Eight one-sided, one generates neither, one unlocated and contradicting located law. `[I]` What *does* exist is a shared **concealment**: `L.2`/`L.3`/`XLIX.5` leave 11 of 14 design laws and 2 of 12 invariants unrealized, so both causes go unreported by the same program. **That is epistemic, not ontological** — proved by the opposite direction of repair. |
| **Q6** | **Can the basis collapse to one?** | **`[F]` NO — COLLAPSE-FAILURE.** Three of four acceptance conditions fail, with three independent proofs: deontic sign (mandate vs prohibition), modality (contingent vs necessary), direction of repair (one act eliminates A and entrenches B). |
| **Q7** | **What is the true ontological floor?** | **`[F]` 2 — FLOOR-2-IRREDUCIBLE.** Ten reduction paths attempted, all documented, all failed. WORLD-A and WORLD-B are both non-empty, which falsifies size-1 as a matter of located state rather than argument. |
| **Q8** | **What is the smallest explanatory set?** | **`[F]` `{META-A, META-B}`, size 2** — META-A the unrealized reconciliation mandate, deepest locus `XI.10` (`CMG-INV-10`), whose verification condition is the validator's own exit status; META-B `ROOT-Ω`, deepest locus `XLIV.7`, origin `CEP-000` §5.4. Coverage: **63 + 37 = 100 of 100** live blocking dependents. |
| **Q9** | **What remains unproven?** | `[UNKNOWN]` (i) whether an entailment edge exists that no located clause expresses — `GAP-R1-01` still holds, there is no entailment register; (ii) whether the 18+18 clause sets are exhaustive — the sweep is pattern-based, and `A-R2-01` was already refuted once by this phase, so under-capture is demonstrated possible; (iii) whether `CMG-INV-10`'s circular verification condition is drafting shorthand or the literal test it reads as; (iv) whether META-A's contingency survives contact with a competent owner, who may hold that `L.2`'s *"and nothing else"* forbids the very check `L.2` contains. |
| **Q10** | **What assumptions still exist?** | `A-R3-01…06` below. The load-bearing one is `A-R3-02`: **the deontic-sign and modal distinctions are derived from located failure modes, not read from a located taxonomy** — `GAP-R2-01` remains open, and this phase adds `GAP-R3-01`. |

---

## SUCCESS CONDITION — DISPOSITION

`[F]` The instruction admitted two outcomes. **The second obtains: Basis = 2, proven irreducible.**

| Required artifact | Delivered |
|---|---|
| complete dependency graph | §R3.1 — `META-A-COMPLETE-MAP`, `META-B-COMPLETE-MAP`; 18 governing clauses each, all dependent classes populated, reproducible sweep digest `6df9d8bb693a7adca57c5383874f6b17`, identical on re-run |
| complete entailment graph | §R3.2 — `ENTAILMENT-A-TO-B` **FALSIFIED**, `ENTAILMENT-B-TO-A` **FALSIFIED**; the entailment relation over `{META-A, META-B}` is **EMPTY** |
| complete counterfactual proof | §R3.6 — `COUNTERFACTUAL-MATRIX`, WORLD-A/B/C/D across five state dimensions |
| complete basis proof | §R3.5 — `MINIMUM-BASIS-PROOF`, 7 steps; §R3.7 — 10 documented failure paths |
| **META-Ω located** | **`[F]` NO — no META-Ω exists in the repository** |

---

## CLASSIFIED RESIDUE

### FACTS `[F]`
1. Of the 14 design laws `XLIX.5` routes to the Article L validator, **11 have zero occurrences in it** — including `CMG-L-04` (META-B's design law) and `CMG-L-10` (META-A's design law). Only `CMG-L-06` (1), `CMG-L-07` (4) and `CMG-L-08` (1) appear.
2. META-A is grounded in **18 located clauses across 4 instruments**, including `CEP-008` `XVI.3`/`XXI.1` and `CEP-004` `X.1`/`XV.3` — **outside `CMG-000001`**, which refutes `A-R2-01` and discharges `UNK-R2-01`.
3. META-B is grounded in **18 located clauses across ≥6 instruments**; `AUTH-06` appears in **258 files**; origin is `CEP-000` §5.4, and `VI.4` records the generalization to `CMG-L-04` explicitly.
4. `register.sh --guard` fails with **exit 3** when regenerated `DATA/REGISTRIES/CONTROL-TOWER/PORTAL` differ from what is committed — regenerate-and-compare, operating at HEAD under the T1 vacancy.
5. `.github/workflows/determinism.yml` runs *"double-build · fail-on-divergence"*, comparing byte-for-byte and exiting non-zero on any divergence.
6. `EXCLUDE_DIR_PREFIXES` excludes **exactly** the directories `register.sh --guard` reconciles, plus `00-MASTER/`: the reconciliation discipline covers the generated layer and not the constitutional layer.
7. `cmg_validate.py` implements `CMG-INV-09` and `CMG-INV-12`, which `L.3` never mandates — exceeding `L.3` requires no amendment.
8. `CMG-INV-01` and `CMG-INV-10` are inside the criteria set `L.2` declares exhaustive, so implementing them engages no amendment path and never reaches `XLII.5`'s *"full ratification path"*.
9. `XII.6` is the only located clause containing both doctrines, and is **necessary for neither**; its two conjuncts carry opposite deontic operators.
10. `CMG-000001` contains **6** `self-elevation`, **1** `self-ratification`, **1** `self-constituting`, and **zero** occurrences of `self-validation`, `self-verification` or `self-certification`.
11. The corpus uses *"self-verifying"* / *"self-certifying"* **54×** approvingly, of content-addressed recomputable evidence, while `CF-C4` voids self-**certification** of authority: the corpus is **opposite-signed** on the two branches of self-reference.
12. `LIII.3`'s fallback for a non-recomputable assertion is *"a judgement with a named accountable authority"*; `CMG-INV-03` is total over 61 concerns and passes, so such an authority is locatable in-corpus.
13. `CEP-006` `I.4` is conditional on finality residing outside the corpus; `CEP-006` `II.4` voids ratification outside authorization.
14. HEAD re-verified: `findings: 0`, `EXIT=0`, `readiness: READY-PROVISIONAL`, over a Registry `XV.5` classifies as invalid.

### INFERENCES `[I]`
1. **A shared concealment is not a shared cause.** Validator non-enforcement hides both meta-causes; enforcement would eliminate one and entrench the other. This single distinction defeated the most attractive collapse candidate.
2. **Co-location in one sentence is not common ancestry.** `XII.6` conjoins the two doctrines and grounds neither.
3. **Deontic sign is the load-bearing asymmetry.** META-A is an unrealized mandate; META-B an in-force prohibition. No single source is both.
4. **Modality is the second asymmetry.** META-A is contingent — asserted achievable by `XV.5` and achieved at HEAD elsewhere. META-B is necessary — *"logically foreclosed."*
5. **META-A is not an absent capability but a misaimed one.** The corpus reconciles what it calls generated and withholds reconciliation from what it calls constitutional, along exactly the `EXCLUDE_DIR_PREFIXES` boundary.
6. **WORLD-B is partially instantiated at HEAD**, which converts ¬(META-B ⇒ META-A) from a counterfactual argument into an observation.
7. Both meta-causes are irreducible; only one is immovable. META-B is fundamental unconditionally; META-A is fundamental but repairable without external authority.
8. The basis has exactly one member of each kind of law the corpus contains — an unrealized mandate and an inescapable prohibition — which is why 2 is the floor.

### ASSUMPTIONS `[A]`
`A-R3-01` The 18+18 clause sets are the located governing sets; both were assembled by pattern sweep plus targeted reading, not by exhaustive clause-by-clause reading of every instrument. `A-R2-01` was refuted by this method, so under-capture is demonstrated possible.
`A-R3-02` The deontic-sign and modal classifications are derived from located clause language (`SHALL` vs `SHALL NOT` / `IS PROHIBITED`) and located failure modes, not from a located taxonomy of obligation types.
`A-R3-03` The candidate ancestor classes are the ten named in the instruction plus `XII.6` and validator non-enforcement, located by search; an unnamed class could exist.
`A-R3-04` Dependent counts (63 / 37 of 100) are inherited from Phases R0/R1 and were not re-derived.
`A-R3-05` `register.sh --guard` and `determinism.yml` were read, not executed; their reconciliation behavior is established from their code and declared contracts, not from an observed failing run.
`A-R3-06` Measured at `1e3e4ba9` in this environment.

### GAPS `[GAP]`
| Id | Gap |
|---|---|
| `GAP-R3-01` | No located instrument classifies constitutional obligations by deontic sign, so no register distinguishes an **unimplemented mandate** from an **in-force prohibition** — the distinction on which the floor rests had to be derived |
| `GAP-R3-02` | `XLIX.5` routes `IV.1` + `CMG-L-01…14` + `CMG-INV-01…12` to the Article L validator while `L.2` restricts its criteria to `CMG-INV-01…12` *"and nothing else"*; the status of the 14 design laws as validator criteria is undetermined by located text |
| `GAP-R3-03` | The reconciliation discipline is implemented on the generated layer and absent on the constitutional layer, and no located instrument records the scope boundary as a decision |
| `GAP-R3-04` | No entailment register exists between meta-causes (`GAP-R1-01` inherited and unresolved at meta rank) |

### UNKNOWNS `[UNKNOWN]`
`UNK-R3-01` Whether the 18+18 clause sets are exhaustive; the sweep is pattern-based and has already been shown to under-capture once.
`UNK-R3-02` Whether a competent owner would read `L.2`'s *"and nothing else"* as **permitting** realization of `CMG-INV-01`/`-10` (this phase's reading, supported by the validator's own `INV-09`/`INV-12` practice) or as **restricting** the validator to `L.3`'s enumerated twelve duties.
`UNK-R3-03` Whether `CMG-INV-10`'s circular verification condition is shorthand for full recomputation or the literal test it reads as; no located record states either.
`UNK-R3-04` Whether the scope inversion at `EXCLUDE_DIR_PREFIXES` was a deliberate exclusion of the constitutional layer from drift enforcement or an unexamined consequence of the registration-universe boundary.
`UNK-R3-05` Whether any third meta-cause is concealed by the same non-enforcement that concealed these two — 11 of 14 design laws are unrealized, and only `CMG-L-04` and `CMG-L-10` were traced here.

---

## PHASE R3 TERMINAL VERDICT

> **DETERMINATION-COMPLETE · COLLAPSE-FAILURE · NO META-Ω IN THE REPOSITORY · FLOOR-2-IRREDUCIBLE · ENTAILMENT RELATION OVER THE BASIS IS EMPTY · META-B FUNDAMENTAL UNCONDITIONALLY · META-A FUNDAMENTAL BUT CONTINGENT**

**`[F]` The size-2 basis was attacked along ten paths and survived all ten.** Both entailment directions are falsified, ten candidate ancestor classes yield zero common ancestors, and no size-1 basis exists. The floor stands at 2 — not by exhaustion of ingenuity but because the corpus contains two categorially different kinds of law, and the basis holds exactly one of each.

**`[I]` The phase's central result is a distinction, not a discovery.** The most attractive collapse was real and had to be refused: `L.2`, `L.3` and `XLIX.5` leave **11 of 14 design laws and 2 of 12 invariants unrealized, including both meta-causes' own design laws** — `CMG-L-10` for META-A and `CMG-L-04` for META-B. One program conceals both. But **enforcement moves them in opposite directions**: implementing the design laws *discharges* META-A's mandate and *hardens* META-B's prohibition. A common cause is removable by one act; these are pushed apart by it. **Validator non-enforcement is a shared concealment, and a shared concealment is not a shared cause.** Every other candidate failed on some form of the same confusion.

**`[F]` The strongest single-source candidate was one sentence, and it grounds neither cause.** `XII.6` says derived truth *"SHALL always be reconciled against repository reality **and** SHALL never be treated as a source of authority"* — META-A and META-B in one clause. It fails because both causes survive its repeal (each is independently grounded 18 ways), because its second conjunct binds only `DECLARATIVE` standing while `CMG-000001` holds `META` at `T1M`, and because its two conjuncts carry **opposite deontic operators**. A sentence may conjoin a mandate and a prohibition. A generating cause cannot be one.

**`[I]` And the intuitive META-Ω — *"self-reference cannot ground"* — is refuted by the corpus's own vocabulary.** The corpus prohibits self-**authority** absolutely: `XLIV.7`, `CEP-000` §5.4, `CMG-L-04`, `IX.16`, `VI.4`, `LXXXIII.3`, `LXXXI.6`, `CF-C4` (*"Self-certification is void"*), `AUTH-06` in 258 files. It **mandates** self-**verification** as a virtue: `CMG-L-10`, `CMG-INV-10`, `XV.5`, `LIII.3`, `LXXXIII.9`, and 54 approving uses of *"self-verifying"* / *"self-certifying"* for content-addressed recomputable evidence. `CMG-000001` contains six prohibitions of self-elevation and **zero** prohibitions of self-verification. A META-Ω voiding self-grounding would have to void five located mandates to explain META-A. **It does not unify the basis; it contradicts half of it.**

**`[F]` The decisive refutation of `META-B ⇒ META-A` is not a counterfactual but an observation.** The regenerate-and-compare discipline whose absence constitutes META-A **is implemented and running at HEAD, under the same T1 vacancy**: `register.sh --guard` exits 3 when regenerated registers diverge from what is committed; `determinism.yml` double-builds and fails on any byte divergence; `ukbx twin --check` certifies the digital twin. And `cmg_validate.py` already implements `CMG-INV-09` and `CMG-INV-12`, which `L.3` never mandates — so exceeding `L.3` needs no amendment, and `CMG-INV-01`/`-10` are already inside the criteria set `L.2` calls exhaustive. **WORLD-B is not hypothetical. It exists in this repository, restricted to the directories the constitutional universe excludes.**

**`[I]` Which yields the sharpest located fact of the phase.** `EXCLUDE_DIR_PREFIXES` excludes exactly `00-BOOK/DATA`, `00-BOOK/REGISTRIES`, `00-BOOK/CONTROL-TOWER`, `00-BOOK/PORTAL` and `00-MASTER/` — **precisely the directories `register.sh --guard` reconciles.** The corpus applies regenerate-and-compare to everything it calls *generated* and withholds it from everything it calls *constitutional*. **META-A is not a missing capability. It is a working capability aimed away from the constitution.**

**`[I]` The two members of the basis are irreducible for different reasons, and the difference matters more than the count.** META-B is fundamental **unconditionally**: doubly proved from disjoint clause sets, 18 clauses across six instruments, *logically foreclosed*, and honestly bounded by the corpus's own phrase — *a missing seed, not a contradiction*. META-A is fundamental **contingently**: irreducible to META-B, derived from nothing located, and yet `XV.5` asserts it achievable and three mechanisms achieve it at HEAD. **One member cannot be moved from inside the corpus. The other has never been tried inside the corpus. Both are irreducible; only one is immovable.**

**This determination falsifies, exhausts and concedes. It recommends nothing, designs nothing, amends nothing, and eliminates nothing.**

---

*PHASE R3 · AUTHORITY = NONE (DERIVED TRUTH) · Reports; determines nothing.*
*`CERTIFIED-PROVISIONAL`; Tier T1 VACANT; `CMG-L-12` applies to every statement herein.*
*Reproduce: `python3 .runtime/phase-r3/r3_maps.py` · `python3 00-CMG/tools/cmg_validate.py --repo-root .`*
